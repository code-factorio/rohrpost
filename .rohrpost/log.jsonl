{"id":"01KZV10H1G5XS7MRHFJCXHFFK3","ts":"2026-08-12T13:01:29.008Z","ticket":"3gg2b1","op":"create","actor":"user/evilissimo@gmail.com","set":{"title":"Fold performance and body storage","type":"epic","status":"open","priority":2}}
{"id":"01KZV11MVQBMAQ0ZZ105RABP4S","ts":"2026-08-12T13:02:05.687Z","ticket":"7ke7wt","op":"create","actor":"user/evilissimo@gmail.com","set":{"title":"Profile fold apply loop","type":"task","status":"open","priority":2,"parent":"3gg2b1","body":"Cold fold at the 30k-event reference is ~109 ms and apply-bound (~85 ms replaying events, ~2.8 us/event; body size adds only ~8 ms). Profile the apply loop before optimizing: find whether ts parsing or per-field dict churn dominates. Measure, then decide."}}
{"id":"01KZV11MZB64D9HAQ1C99BMT2C","ts":"2026-08-12T13:02:05.803Z","ticket":"jdt9c2","op":"create","actor":"user/evilissimo@gmail.com","set":{"title":"Implement size-triggered body spill","type":"task","status":"open","priority":2,"parent":"3gg2b1","body":"Per spec section 13.2: bodies stay inline; a body exceeding a configurable threshold (default 4096 bytes) spills to bodies/<id>.md on write, and rp show resolves either form transparently. Section 5.3 already permits both shapes, so no schema change. Include a doctor check that every referenced sidecar file exists. Threshold tuning should use the by-type body percentiles from the rp stats ticket once a real corpus exists."}}
{"id":"01KZV11N30M548YKQ3FB7D0GZX","ts":"2026-08-12T13:02:05.920Z","ticket":"ytewxe","op":"create","actor":"user/evilissimo@gmail.com","set":{"title":"Set-wise three-way merge for labels","type":"task","status":"open","priority":2,"parent":"3gg2b1","body":"Known phase-1 gap. Labels (and other set fields) currently merge by per-field last-write-wins, which silently loses concurrent set additions from two runners. Implement set-wise three-way merge so concurrent labels+ ops compose."}}
{"id":"01KZV11N83PGDTK870BVG6ZGB3","ts":"2026-08-12T13:02:06.083Z","ticket":"pnq8vd","op":"create","actor":"user/evilissimo@gmail.com","set":{"title":"rp stats command","type":"task","status":"open","priority":2,"parent":"3gg2b1","body":"Extend rp stats to report body-size percentiles split by ticket type (epic vs task), so the bimodal body distribution behind the section 13.2 spill decision can be observed. This supplies signals D1/D2 once a real corpus exists and tunes the 4096-byte spill threshold."}}
{"id":"01KZV30Q2Z9WS7YMG0SZ8HFGTF","ts":"2026-08-12T13:36:32.351Z","ticket":"7hwnnt","op":"create","actor":"user/evilissimo@gmail.com","set":{"title":"Compaction loses events if interrupted between log rewrite and archive append","type":"bug","status":"open","priority":1,"body":"src/rohrpost/compact.py run() rewrites (truncates) log.jsonl to only the kept events (_rewrite_log at :177) BEFORE appending the archived events to the archive buckets (the _append_archive loop at :178-179). A crash or SIGKILL between those two steps permanently loses the to-be-archived events: in that window they are removed from the log but not yet present in any archive file, existing only in the in-memory archive_buckets dict. Spec §6.1 specifies the crash-safe order - archive first (step 2), then rewrite the log (step 3) - which leaves only dedupable duplicates on a crash (the fold drops them by event id). Fix: append to archive files first, then rewrite the log, all still under store.file_lock. Add a regression test that simulates an interruption between the steps and asserts no events are lost."}}
{"id":"01KZV30Q6YS0KESSW54W1FV03N","ts":"2026-08-12T13:36:32.478Z","ticket":"d0v795","op":"create","actor":"user/evilissimo@gmail.com","set":{"title":"Doctor authentication probes for all providers","type":"epic","status":"open","priority":2,"body":"rp doctor (spec §10.1) must verify remote credentials actually authenticate, not merely that a token env var is present. GitHub already has a real probe (providers/github.py + the github branch of doctor.py _remote_authenticated). The generic fallback (doctor.py _remote_authenticated, ~:206-215) only checks token presence and returns True for a present-but-invalid or expired token, so a configured non-github remote with a dead token passes the check. Each provider needs its own authentication probe in rp doctor. Tracked per provider below."}}
{"id":"01KZV314RQH5EX57BZTH54RHVF","ts":"2026-08-12T13:36:46.359Z","ticket":"eawm6q","op":"create","actor":"user/evilissimo@gmail.com","set":{"title":"Doctor authentication probe: GitLab","type":"task","status":"open","priority":2,"parent":"d0v795","body":"Add a real authentication probe for the GitLab remote to rp doctor, so a present-but-invalid or expired token is reported as failing rather than passing. Today the generic _remote_authenticated (doctor.py, ~:206-215) only checks token presence for non-github remotes (spec audit finding #4), returning True for a dead token. Depends on the GitLab provider landing (phase 3)."}}
{"id":"01KZV314W9AG6ED9W1PWF6SFAZ","ts":"2026-08-12T13:36:46.473Z","ticket":"9196c7","op":"create","actor":"user/evilissimo@gmail.com","set":{"title":"Doctor authentication probe: Jira","type":"task","status":"open","priority":2,"parent":"d0v795","body":"Add a real authentication probe for the Jira remote to rp doctor, so a present-but-invalid or expired token is reported as failing rather than passing. Today the generic _remote_authenticated (doctor.py, ~:206-215) only checks token presence for non-github remotes (spec audit finding #4), returning True for a dead token. Depends on the Jira provider landing (phase 3)."}}
{"id":"01KZV315004XF0ZAK95GG0TKT1","ts":"2026-08-12T13:36:46.592Z","ticket":"qhh9yk","op":"create","actor":"user/evilissimo@gmail.com","set":{"title":"Doctor authentication probe: Linear","type":"task","status":"open","priority":2,"parent":"d0v795","body":"Add a real authentication probe for the Linear remote to rp doctor, so a present-but-invalid or expired token is reported as failing rather than passing. Today the generic _remote_authenticated (doctor.py, ~:206-215) only checks token presence for non-github remotes (spec audit finding #4), returning True for a dead token. Depends on the Linear provider landing (phase 3)."}}
{"id":"01KZV3A75DV0A780XJ5YYAVSNV","ts":"2026-08-12T13:41:43.725Z","ticket":"322zs1","op":"create","actor":"user/evilissimo@gmail.com","set":{"title":"Body-merge conflicts must keep conflict markers in the body","type":"bug","status":"open","priority":2,"body":"Spec §8.3 requires that on a body-merge collision the conflict markers (<<<<<<< / ======= / >>>>>>>) are kept IN the body and the ticket is flagged. The code computes the marker-laden three-way merge in _merge_body (merge.py ~:120-130) but then DISCARDS it, recording only the original local/remote values in a FieldConflict; _flag_conflict (sync.py ~:234-267) writes status=review + a conflict:<remote> label + a comment, but never updates the body - so the markers a human or agent needs in order to resolve the conflict are lost. The merge.py module docstring (~:17-18) already claims \"keeping conflict markers on collision\", which the flag path does not do. Fix: under the default flag policy, write the marker-laden merged text to the body in addition to the existing label/comment. Add coverage asserting the markers survive in the body (current tests/test_merge.py:59-64 only checks the flag, not the markers)."}}
{"id":"01KZV61WTD4E562CWW8WWZ62TA","ts":"2026-08-12T14:29:36.717Z","ticket":"__sync__","op":"synced","actor":"remote/github","remote":"github","at":"2026-08-12T14:29:36.717Z"}
{"id":"01KZV63089BY6W3RMY6DX7DCPH","ts":"2026-08-12T14:30:13.001Z","ticket":"__sync__","op":"synced","actor":"remote/github","remote":"github","at":"2026-08-12T14:30:13.001Z"}
{"id":"01KZV6VM4GB9GMWJJDQT4SQD7Q","ts":"2026-08-12T14:43:39.792Z","ticket":"3gg2b1","op":"link","actor":"user/evilissimo@gmail.com","remote":"github","ref":"2"}
{"id":"01KZV6VNHBFZ4GDXSKG2H2YAXQ","ts":"2026-08-12T14:43:41.227Z","ticket":"7ke7wt","op":"link","actor":"user/evilissimo@gmail.com","remote":"github","ref":"3"}
{"id":"01KZV6VPGPNRAPHMJXX5EKYDYH","ts":"2026-08-12T14:43:42.230Z","ticket":"jdt9c2","op":"link","actor":"user/evilissimo@gmail.com","remote":"github","ref":"4"}
{"id":"01KZV6VQM0Y0MS3V9SAG7TTJAC","ts":"2026-08-12T14:43:43.360Z","ticket":"ytewxe","op":"link","actor":"user/evilissimo@gmail.com","remote":"github","ref":"5"}
{"id":"01KZV6VRMB5WWQNTWTJKBCV6EV","ts":"2026-08-12T14:43:44.395Z","ticket":"pnq8vd","op":"link","actor":"user/evilissimo@gmail.com","remote":"github","ref":"6"}
{"id":"01KZV6VTANS7YHAE1E7H2E8ZS4","ts":"2026-08-12T14:43:46.133Z","ticket":"7hwnnt","op":"link","actor":"user/evilissimo@gmail.com","remote":"github","ref":"7"}
{"id":"01KZV6VVDVRYTF67AC0PV9H81G","ts":"2026-08-12T14:43:47.259Z","ticket":"d0v795","op":"link","actor":"user/evilissimo@gmail.com","remote":"github","ref":"8"}
{"id":"01KZV6VWCS3YKT9HCRBN00X48A","ts":"2026-08-12T14:43:48.249Z","ticket":"eawm6q","op":"link","actor":"user/evilissimo@gmail.com","remote":"github","ref":"9"}
{"id":"01KZV6VXDMQCAM0XVBBMFTSKNQ","ts":"2026-08-12T14:43:49.300Z","ticket":"9196c7","op":"link","actor":"user/evilissimo@gmail.com","remote":"github","ref":"10"}
{"id":"01KZV6VYCB425P6QAKHQ358KEA","ts":"2026-08-12T14:43:50.283Z","ticket":"qhh9yk","op":"link","actor":"user/evilissimo@gmail.com","remote":"github","ref":"11"}
{"id":"01KZV6VZRAS4A6R3N1W16AXV7Y","ts":"2026-08-12T14:43:51.690Z","ticket":"322zs1","op":"link","actor":"user/evilissimo@gmail.com","remote":"github","ref":"12"}
{"id":"01M0F2MSR921P2Q3BDRENWFFRE","ts":"2026-08-20T07:54:50.505Z","ticket":"3er9f4","op":"create","actor":"runner/copilot@wayfinder","set":{"title":"Human-addressable tickets: how a person points at a Rohrpost ticket","type":"epic","status":"open","priority":2,"labels+":["wayfinder:map"],"body":"## Destination\n\nA locked decision on **how a human addresses a Rohrpost ticket**, recorded as an ADR plus an\namendment to spec §5.1 (Identifiers) and §10 (CLI), ready to hand to a build session. The\ndecision, not the code: implementation is handed off once the way is clear.\n\n## Notes\n\n**Domain.** Rohrpost's id/addressing ergonomics, driven by `/wayfinder` being awkward to\ndrive by hand: `RP-jdt9c2` is hard to type and impossible to hold in your head.\n\n**Skills.** Call `grilling` and `domain-modeling` every session. Use `research` for facts\noutside this repo. No `CONTEXT.md` exists yet — create one when the first term is resolved.\n\n**Plan, don't do.** The destination is a decision, not shipped code.\n\n**Standing constraints, settled while charting:**\n\n- **Agent-first holds** (`AGENTS.md`). Human affordances live on the *input* side only — what\n  `rp` accepts. What agents emit and consume does not change; `rp ready --json` stays fast\n  and small (§10).\n- **Ambiguity is a hard error** (`exit 1`), never a guess. Determinism is worth more to an\n  agent than convenience is to a human.\n- **The stored id form is fixed** at 6 lowercase base32 chars (§5.1). Only the *addressing\n  layer* is in play — what `rp` accepts as input and what it renders.\n- **\"Hard to type\" and \"hard to reason about\" are two problems.** Typing is input\n  ergonomics. Reasoning is a rendering-and-prose problem: 6 random characters can never be\n  memorable, so the fix is to keep bare ids out of the human's face, not to make them\n  prettier.\n\n## Decisions so far\n\n<!-- one line per closed ticket; zoom the link for the detail it holds -->\n\n## Not yet specified\n\n- **The write-up itself**: which ADR, and the exact §5.1 / §10 wording. Waits on the\n  addressing mechanism being settled.\n- **Shell completion**: whether it is part of the answer, and for which shells. None exists\n  today. Waits on the moments audit.\n- **A positional handle** over the last result set (`rp show @2` against `rp ready`). Waits\n  on the moments audit and on the rendering question.\n- **Abbreviation durability**: what happens when an abbreviation that was unique becomes\n  ambiguous as tickets accumulate, and whether an abbreviation may ever be *written down*\n  (commit message, ticket body, map link) rather than merely typed. Waits on the resolution\n  mechanism.\n- **Knock-on to the skill's own output**: whether any of this changes the link form\n  `/wayfinder` writes into a map's Decisions-so-far. Waits on the map-addressing decision.\n\n## Out of scope\n\n- **Changing the stored id form** — sequential numbers, counters, or title-derived slugs.\n  Spec §5.1 rejects these deliberately: they need either central coordination (which this\n  design exists to avoid) or mutability. Reopening this would redraw the destination, and\n  that is a fresh effort, not a resumption."}}
{"id":"01M0F2QE2NQNZ83R65CAFT26EN","ts":"2026-08-20T07:56:16.853Z","ticket":"p2amzn","op":"create","actor":"runner/copilot@wayfinder","set":{"title":"Which human moments actually require typing a ticket id?","type":"task","status":"open","priority":0,"parent":"3er9f4","labels+":["wayfinder:grilling"],"body":"## Question\n\nWhere does a human *actually* have to produce a ticket id, and where do they only think they\ndo? Everything else on this map is sized by the answer, so it goes first.\n\nThe whole effort rests on the premise that humans type ids. That premise is untested. `rp`\ntakes `<id>` positionally on eleven commands (§10), but this repo is agent-first: most of\nthose calls are made by agents that just read the id out of a previous `--json` response and\nnever type anything. If the human's real path is `rp ready` → copy → paste, then typing is\nnot the problem and this map should shrink drastically.\n\nAudit the moments and classify each one:\n\n- **Copy-paste** — the id is already on screen from a previous command. No typing.\n- **Transcribe** — the id is on screen but in a different surface (a browser, a chat message,\n  the map body, a PR) and has to be retyped by hand.\n- **Recall** — no id on screen at all; the human has to *know* which ticket they mean and go\n  find it.\n\nThe last two are the expensive ones, and they may want different answers: transcription is a\ncharacter-accuracy problem, recall is a search problem.\n\nConcrete moments to test, at minimum: invoking `/wayfinder` with a map; picking a ticket off\n`rp ready` to claim; `rp show`-ing something a colleague or agent mentioned by name;\nreferring to a ticket in a commit message; jumping back to a ticket the day after.\n\n**Resolved when** the moments are enumerated and classified, and it is clear which class the\neffort is actually optimising for — including the possible answer that typing is rare enough\nthat only the recall path deserves work."}}
{"id":"01M0F2QE69TYJJ508B7542ZYDV","ts":"2026-08-20T07:56:16.969Z","ticket":"twaq4c","op":"create","actor":"runner/copilot@wayfinder","set":{"title":"How comparable tools make random ids addressable","type":"task","status":"open","priority":1,"parent":"3er9f4","labels+":["wayfinder:research"],"body":"## Question\n\nHow do comparable tools let a human address an object whose identifier is random? Rohrpost is\nnot the first to choose uncoordinated random ids and then have to hand them to a person, and\nthe prior art is unusually rich. Gather it before designing anything.\n\nCover at least:\n\n- **Jujutsu (`jj`)** — the closest analogue by far. Random change ids, resolved by unique\n  prefix, and it goes further: it renders the distinguishing prefix in a different colour so\n  the human can see *how much* of the id they need to type, and it deliberately draws change\n  ids from a different alphabet than commit hashes so the two can never be confused. Report\n  the shortest-prefix rules, the ambiguity behaviour, and how prefixes are kept stable.\n- **Git** — `--abbrev` / `core.abbrev`, the auto-scaling minimum length as a repo grows, and\n  what happens when an abbreviation goes ambiguous. Also `@{-1}`, `HEAD~`, and refnames as\n  the \"don't type a hash at all\" escape hatch.\n- **Sapling / Mercurial** — local sequential revision numbers alongside global hashes, and\n  why Mercurial ultimately regretted them.\n- **`gh` and Linear/Jira** — short sequential keys, and what coordination they pay for it.\n- **`beads` / `seeds`** — named in Appendix A as the design ancestors; what they do here.\n\nFor each, record the mechanism, the ambiguity behaviour, and the cost. The output is\nevidence, not a recommendation.\n\n**Resolved when** each tool's mechanism is written up with sources, and the trade-offs are\nstated in terms that the prefix and title-resolution tickets can consume directly."}}
{"id":"01M0F2QE9BNJD1NQ20DGZNFEGP","ts":"2026-08-20T07:56:17.067Z","ticket":"dcdqj6","op":"create","actor":"runner/copilot@wayfinder","set":{"title":"Should rp resolve a ticket from an id prefix?","type":"task","status":"open","priority":2,"parent":"3er9f4","labels+":["wayfinder:grilling"],"body":"## Question\n\nShould `rp` accept a **prefix** of a ticket id wherever it accepts an id — `rp show jdt`\nresolving `jdt9c2` — and if so, under exactly what rules?\n\nThis is the direct answer to \"hard to type\", and the seam is clean: `normalize_id` in\n`ids.py` is the single chokepoint every command already funnels through. But the details are\nwhere it lives or dies:\n\n- **Minimum length.** One character is unusable; three is git-ish. Does the floor scale with\n  ticket count, as git's does, or stay fixed?\n- **Ambiguity.** The map's standing constraint says a hard `exit 1`. Confirm that, and decide\n  what the error *says* — listing the candidate titles turns a failure into the search the\n  human actually wanted.\n- **Interaction with the display prefix.** `RP-jdt` — does the rendered form accept\n  abbreviation too? `normalize_id` currently splits on the last `-` and demands exactly six\n  characters after it.\n- **Interaction with sync.** Does an abbreviation ever reach a remote, a shadow file, or the\n  event log? It must not — the log stores bare ids (§5.2) — so confirm the resolution happens\n  at the CLI boundary and nowhere deeper.\n- **The agent-first tax.** Agents pass full ids and would now pay a lookup that can fail in a\n  new way. Is the resolution cost real against a folded ticket set, and does it touch\n  `rp ready`?\n\n**Resolved when** the rule is stated precisely enough to implement, including the minimum\nlength, the ambiguity behaviour and message, and the boundary at which resolution happens."}}
{"id":"01M0F2QKY8WB3S7MD0NBKZ9EX2","ts":"2026-08-20T07:56:22.856Z","ticket":"keswrj","op":"create","actor":"runner/copilot@wayfinder","set":{"title":"Should rp resolve a ticket by title?","type":"task","status":"open","priority":2,"parent":"3er9f4","labels+":["wayfinder:grilling"],"body":"## Question\n\nShould `rp` resolve a ticket from its **title** — `rp show \"compaction loses\"` — wherever it\naccepts an id?\n\nThis is the answer to the *other* half of the problem. Humans remember \"the compaction bug\",\nnever `RP-7hwnnt`, and the wayfinder skill's \"Refer by name\" rule already insists that\neverything a human reads uses titles. If the skill hands the human a name and the tool only\naccepts an id, the human is left doing the translation by hand — which is precisely the\nfriction that started this map.\n\nThe tension is that title matching sits awkwardly against agent-first determinism:\n\n- Titles are **mutable**. An id is forever; a title changes under `rp set <id> title=...`, so\n  a title reference can silently stop resolving — or worse, start resolving to something\n  else.\n- Titles are **not unique**, and matching is a spectrum: exact, substring, case-insensitive,\n  fuzzy. Each step up is friendlier and less predictable.\n- It **collides with prefix resolution**. If `rp show foo` can mean either an id prefix or a\n  title substring, the grammar is ambiguous by construction. Options: a distinguishing sigil,\n  a separate flag (`rp show --title \"...\"`), a search subcommand that prints ids rather than\n  resolving them, or ruling one of the two out entirely.\n\nNote the escape hatch: a *search* command that prints candidates and makes the human pick is\nstrictly weaker than resolution, but it is deterministic, cannot mis-resolve, and may cover\nthe recall moment completely.\n\n**Resolved when** title-based addressing is either specified precisely (match semantics, tie\nhandling, how it is distinguished from an id) or ruled out with the reason recorded."}}
{"id":"01M0F2QM1ZJ47H1QKES1CY87MK","ts":"2026-08-20T07:56:22.975Z","ticket":"9ckb0z","op":"create","actor":"runner/copilot@wayfinder","set":{"title":"How a human starts a wayfinder session without typing an id","type":"task","status":"open","priority":1,"parent":"3er9f4","labels+":["wayfinder:grilling"],"body":"## Question\n\nHow does a human start a `/wayfinder` session without typing an id at all?\n\nThis is the user's original complaint at its narrowest, and it may be answerable without\ntouching `rp`. The skill's \"Work through the map\" mode is invoked \"with a map (URL or\nnumber)\" — but a Rohrpost map is not just any ticket: it is an **epic labelled\n`wayfinder:map`**, which makes it findable by query. `rp list --label wayfinder:map --json`\nalready returns exactly the maps, and if there is only one open map the human need supply\nnothing.\n\nDecide:\n\n- **The zero-argument case.** With exactly one open map, does `/wayfinder` just take it? That\n  is the common case and removes the id entirely.\n- **Disambiguation.** With several open maps, what does the human type? A number from a\n  printed menu is typeable and unambiguous but positional and unstable across invocations; a\n  title fragment is stable and memorable but overlaps the title-resolution question.\n- **Pointing at a specific child ticket.** The skill lets the user name a ticket rather than\n  taking the frontier's first. Same question one level down, but the candidate set is small\n  and already scoped to the map, which makes cheap answers viable here that would not be\n  safe repo-wide.\n- **The doc surface.** Whatever is decided lands in the \"Wayfinding operations\" section of\n  `docs/agents/issue-tracker.md`, which currently tells the agent to pass a map id around.\n\nWatch the boundary: if the answer needs general title resolution in `rp`, this ticket has\ngrown into the title-resolution question and should defer to it rather than pre-empt it.\n\n**Resolved when** the invocation contract is specified — what the human says to reach a map\nand a ticket within it — and it is clear whether it needs anything from `rp` core."}}
{"id":"01M0F2QM5BM02PNR2NYQ34JNW2","ts":"2026-08-20T07:56:23.083Z","ticket":"k4r6ta","op":"create","actor":"runner/copilot@wayfinder","set":{"title":"What rp renders when a human, not an agent, is reading","type":"task","status":"open","priority":3,"parent":"3er9f4","labels+":["wayfinder:prototype"],"body":"## Question\n\nWhat should `rp` put on screen when the reader is a **human**, so that ids stay out of the\nway?\n\nThe map holds that \"hard to reason about\" is a rendering problem, not an id problem: six\nrandom characters will never be memorable, so the fix is to stop making a person look at\nthem. `rp` already has two audiences and one of them is explicitly served — `--json` is the\nagent interface and \"the plain output is for human terminals\" — but the plain output has\nnever been designed *as* a human surface.\n\nMake it concrete rather than arguing it. Prototype the human-facing rendering of the calls a\nperson actually runs by hand — `rp ready`, `rp list`, `rp tree`, `rp show` — and react to it:\n\n- Where does the id belong: leading column, trailing dim text, or absent until needed?\n- If prefix resolution lands, should the distinguishing prefix be visually marked, the way\n  `jj` highlights the part you need to type? That turns every listing into a lesson in what\n  is safe to type.\n- `rp tree` is the map's whole-map view. Does an epic and its children read as a *map* today,\n  or as a flat dump with indentation?\n- Does anything here justify colour, given `NO_COLOR` is respected (§10)?\n\nKeep it cheap and throwaway: static mock output pasted side by side is enough to judge, and\nfaster than wiring a renderer.\n\n**Resolved when** a preferred human rendering exists as a concrete artifact, linked from this\nticket, with the reasoning for what was kept and what was cut."}}
{"id":"01M0F2QZDZ038WM1MW7KPM41YF","ts":"2026-08-20T07:56:34.623Z","ticket":"dcdqj6","op":"set","actor":"runner/copilot@wayfinder","set":{"blocked_by+":["p2amzn","twaq4c"]}}
{"id":"01M0F2QZHDTKQVGRYQ3W9RRPGR","ts":"2026-08-20T07:56:34.733Z","ticket":"keswrj","op":"set","actor":"runner/copilot@wayfinder","set":{"blocked_by+":["p2amzn","twaq4c"]}}
{"id":"01M0F2QZMTTFMTPE7Q129ZN8WX","ts":"2026-08-20T07:56:34.842Z","ticket":"k4r6ta","op":"set","actor":"runner/copilot@wayfinder","set":{"blocked_by+":["p2amzn"]}}
{"id":"01M0F2SXYEKPEZ63WC7TX8M2JQ","ts":"2026-08-20T07:57:38.638Z","ticket":"twaq4c","op":"set","actor":"runner/copilot@wayfinder","set":{"status":"in_progress","assignee":"runner/copilot@wayfinder"}}
{"id":"01M0F3MFB7GF6XZ3S6946FZMAF","ts":"2026-08-20T08:12:08.423Z","ticket":"twaq4c","op":"comment","actor":"runner/copilot@wayfinder","text":"Resolved. Full write-up with sources: `docs/research/id-addressing-prior-art.md`.\n\n**The mechanism every comparable tool converged on.** Prefix resolution is not a novel idea to\nbe weighed — it is what *every* surveyed tool with random ids and human users implements: jj,\ngit, Mercurial, Sapling, and beads. All of them treat ambiguity as a hard error, which\nindependently confirms the standing constraint this map already adopted.\n\n**Findings that bear directly on the open tickets:**\n\n- **jj scopes resolution.** `IdPrefixContext` resolves a prefix inside a configured revset\n  first (defaulting to what `jj log` shows) and only falls back to the whole repository on a\n  miss. Short prefixes therefore stay short, because unrelated history cannot lengthen them.\n  The Rohrpost analogue — resolve within open tickets first, then all tickets — is a real\n  option the prefix ticket did not anticipate. It costs context dependence: the prefix you\n  need changes as the scope changes.\n- **jj also renders the answer.** The default template shows at least 8 characters but styles\n  the *distinguishing* prefix differently from the padding, so every listing teaches the\n  reader how much they actually need to type. That is the tightest coupling found between the\n  typing problem and the rendering problem.\n- **git scales its floor.** `b = floor(log2 N)+1`, then at least `ceil(b/2)` characters, floor\n  of 7 — a birthday bound, not a guess. It answers the \"fixed or scaling minimum\" question\n  with a formula. Note Rohrpost's collision domain is ~2^30 over one repo, far smaller than\n  git's object count, so the arithmetic differs even if the approach transfers.\n- **git and beads both print the candidates** on ambiguity, with titles/subjects. Both judged\n  it worth the code — it converts a dead end into the search the human wanted.\n- **Title matching is never identity.** Not in a single surveyed tool. The two that offer it\n  keep it in a separate, list-returning command (`sd search`), or make the multiple-match rule\n  explicit and positional (`git :/regex` takes the youngest match). This is the strongest\n  single signal in the survey and the title ticket should start from it.\n- **Sequential keys always have an allocator.** GitHub, Linear, and Jira can afford `ENG-123`\n  only because creation passes through one central service. Mercurial, which allocated locally\n  instead, documents the result as a defect: revnums are \"strictly local convenience\n  identifiers\" and \"Do not use them to talk about changesets with other people.\" This is\n  first-party confirmation of the out-of-scope call on this map — the alternative was tried\n  and its own maintainers warn against it.\n- **Seeds, the nearest ancestor, requires exact ids** and has no id completion at all. Its\n  shell completion covers commands and flags only. So the ancestor does not solve this, and\n  there is nothing to inherit."}
{"id":"01M0F3MFG73QB6RCGMVB982K55","ts":"2026-08-20T08:12:08.583Z","ticket":"twaq4c","op":"set","actor":"runner/copilot@wayfinder","set":{"status":"done"},"reason":"Prior art gathered in docs/research/id-addressing-prior-art.md: every comparable tool with random ids implements unique-prefix resolution with hard-fail ambiguity; no tool treats a title as identity."}
{"id":"01M0F3NVEP8RY0TVAQ2ZK6GT2T","ts":"2026-08-20T08:12:53.590Z","ticket":"3er9f4","op":"set","actor":"runner/copilot@wayfinder","set":{"body":"## Destination\n\nA locked decision on **how a human addresses a Rohrpost ticket**, recorded as an ADR plus an\namendment to spec §5.1 (Identifiers) and §10 (CLI), ready to hand to a build session. The\ndecision, not the code: implementation is handed off once the way is clear.\n\n## Notes\n\n**Domain.** Rohrpost's id/addressing ergonomics, driven by `/wayfinder` being awkward to\ndrive by hand: `RP-jdt9c2` is hard to type and impossible to hold in your head.\n\n**Skills.** Call `grilling` and `domain-modeling` every session. Use `research` for facts\noutside this repo. No `CONTEXT.md` exists yet — create one when the first term is resolved.\n\n**Plan, don't do.** The destination is a decision, not shipped code.\n\n**Standing constraints, settled while charting:**\n\n- **Agent-first holds** (`AGENTS.md`). Human affordances live on the *input* side only — what\n  `rp` accepts. What agents emit and consume does not change; `rp ready --json` stays fast\n  and small (§10).\n- **Ambiguity is a hard error** (`exit 1`), never a guess. Determinism is worth more to an\n  agent than convenience is to a human. *(Since confirmed by prior art: every comparable tool\n  does exactly this.)*\n- **The stored id form is fixed** at 6 lowercase base32 chars (§5.1). Only the *addressing\n  layer* is in play — what `rp` accepts as input and what it renders.\n- **\"Hard to type\" and \"hard to reason about\" are two problems.** Typing is input\n  ergonomics. Reasoning is a rendering-and-prose problem: 6 random characters can never be\n  memorable, so the fix is to keep bare ids out of the human's face, not to make them\n  prettier.\n\n## Decisions so far\n\n<!-- one line per closed ticket; zoom the link for the detail it holds -->\n\n- [How comparable tools make random ids addressable](RP-twaq4c): every tool with random ids\n  and human users (jj, git, hg, Sapling, beads) implements **unique-prefix resolution with\n  hard-fail ambiguity**, and **no** tool treats a title as identity — title lookup is always a\n  separate, list-returning search. Write-up:\n  [`docs/research/id-addressing-prior-art.md`](docs/research/id-addressing-prior-art.md).\n\n## Not yet specified\n\n- **The write-up itself**: which ADR, and the exact §5.1 / §10 wording. Waits on the\n  addressing mechanism being settled.\n- **Shell completion**: whether it is part of the answer, and for which shells. None exists\n  today, and neither does it in `seeds`. Waits on the moments audit.\n- **A positional handle** over the last result set (`rp show @2` against `rp ready`). Waits\n  on the moments audit and on the rendering question.\n- **Knock-on to the skill's own output**: whether any of this changes the link form\n  `/wayfinder` writes into a map's Decisions-so-far. Waits on the map-addressing decision.\n\n## Out of scope\n\n- **Changing the stored id form** — sequential numbers, counters, or title-derived slugs.\n  Spec §5.1 rejects these deliberately: they need either central coordination (which this\n  design exists to avoid) or mutability. The prior-art research closed this off further:\n  every tool with short sequential keys (GitHub, Linear, Jira) pays for a central allocator,\n  and Mercurial, which allocated locally instead, warns in its own docs that the result must\n  not be used to talk to other people. Reopening this would redraw the destination, and that\n  is a fresh effort, not a resumption."}}
{"id":"01M0F3NVK9ZDWY52TE9NZ385SY","ts":"2026-08-20T08:12:53.737Z","ticket":"dcdqj6","op":"set","actor":"runner/copilot@wayfinder","set":{"body":"## Question\n\nShould `rp` accept a **prefix** of a ticket id wherever it accepts an id — `rp show jdt`\nresolving `jdt9c2` — and if so, under exactly what rules?\n\nThis is the direct answer to \"hard to type\", and the seam is clean: `normalize_id` in\n`ids.py` is the single chokepoint every command already funnels through. Prior art settles\n*whether* (every comparable tool does it); what remains is *how*:\n\n- **Minimum length.** One character is unusable; three is git-ish. Does the floor scale with\n  ticket count, as git's does (`b = floor(log2 N)+1`, then at least `ceil(b/2)`, floor of 7),\n  or stay fixed? Note Rohrpost's collision domain is ~2^30 across one repo — far smaller than\n  git's object count — so the formula does not transfer unexamined.\n- **Ambiguity.** The map's standing constraint says a hard `exit 1`. Confirm that, and decide\n  what the error *says*: git and beads both print the candidate list with titles, and both\n  judged it worth the code because it turns a dead end into the search the human wanted.\n- **Scope of resolution.** `jj` resolves a prefix within a configured subset first (what\n  `jj log` shows) and only falls back to the whole store on a miss, which keeps prefixes\n  short as history grows. The Rohrpost analogue is resolving within **open** tickets before\n  all tickets. It buys brevity and costs context dependence — the prefix you need changes as\n  the scope changes. Decide whether that trade is worth it here, where the whole store is\n  small and already folded into memory.\n- **Interaction with the display prefix.** `RP-jdt` — does the rendered form accept\n  abbreviation too? `normalize_id` currently splits on the last `-` and demands exactly six\n  characters after it.\n- **Durability, and whether an abbreviation may be written down.** An abbreviation that is\n  unique today can go ambiguous as tickets accumulate. Git accepts this because a hash is\n  recoverable; Mercurial's warning about local identifiers is the cautionary case. Decide\n  whether an abbreviation is strictly a *typing* convenience — never valid in a commit\n  message, ticket body, or map link — or a durable reference. If the former, say so where a\n  human will read it.\n- **Interaction with sync.** An abbreviation must never reach a remote, a shadow file, or the\n  event log, which stores bare ids (§5.2). Confirm resolution happens at the CLI boundary and\n  nowhere deeper.\n- **The agent-first tax.** Agents pass full ids and would now pay a lookup that can fail in a\n  new way. Is the resolution cost real against a folded ticket set, and does it touch\n  `rp ready`? Beads resolves exact matches ahead of prefix matches, which keeps the full-id\n  path free.\n\n**Resolved when** the rule is stated precisely enough to implement: minimum length, ambiguity\nbehaviour and message, resolution scope, the boundary at which resolution happens, and\nwhether an abbreviation may be durably written down."}}
{"id":"01M0FK9K31C3PENV79QP1QJBKK","ts":"2026-08-20T12:45:49.026Z","ticket":"3er9f4","op":"set","actor":"runner/copilot@wayfinder","set":{"title":"[addr] Human-addressable tickets: how a person points at a Rohrpost ticket"}}
{"id":"01M0FK9K636RJX1T84NQX5XSVA","ts":"2026-08-20T12:45:49.123Z","ticket":"p2amzn","op":"set","actor":"runner/copilot@wayfinder","set":{"title":"[addr-1] Which human moments actually require typing a ticket id?"}}
{"id":"01M0FK9K93TEYCE7783GT57YKY","ts":"2026-08-20T12:45:49.219Z","ticket":"twaq4c","op":"set","actor":"runner/copilot@wayfinder","set":{"title":"[addr-2] How comparable tools make random ids addressable"}}
{"id":"01M0FK9KCJ0YHG7TPCDKQJV95T","ts":"2026-08-20T12:45:49.330Z","ticket":"9ckb0z","op":"set","actor":"runner/copilot@wayfinder","set":{"title":"[addr-3] How a human starts a wayfinder session without typing an id"}}
{"id":"01M0FK9KFX39K6BMPZZF55EDC1","ts":"2026-08-20T12:45:49.437Z","ticket":"dcdqj6","op":"set","actor":"runner/copilot@wayfinder","set":{"title":"[addr-4] Should rp resolve a ticket from an id prefix?"}}
{"id":"01M0FK9KK6E6607R4T413BNAEE","ts":"2026-08-20T12:45:49.542Z","ticket":"keswrj","op":"set","actor":"runner/copilot@wayfinder","set":{"title":"[addr-5] Should rp resolve a ticket by title?"}}
{"id":"01M0FK9KPDVZMCGHVXZP1N3RC3","ts":"2026-08-20T12:45:49.645Z","ticket":"k4r6ta","op":"set","actor":"runner/copilot@wayfinder","set":{"title":"[addr-6] What rp renders when a human, not an agent, is reading"}}
{"id":"01M0FKAGRJ5J8DKHEM829PRGCB","ts":"2026-08-20T12:46:19.410Z","ticket":"keswrj","op":"comment","actor":"runner/copilot@wayfinder","text":"**No — and no alias field either.** Titles are **search keys, never identities**. The 6-char\nbase32 id remains the only identity a ticket has.\n\n## What was decided\n\nA human handle is encoded as a **leading bracketed prefix in the title**:\n\n```\n[addr-2] How comparable tools make random ids addressable\n```\n\nThe prefix (`addr`) is agreed with the user when the map is charted; the sequence is\nallocated by the charting session, which creates the tickets serially in one process. The\nbare prefix `[addr]` titles the map epic itself, so `/wayfinder addr` reaches it.\n\n`rp` learns **nothing** about this convention. No new field, no `SET_FIELDS` entry, no fold\nchange, no `_fieldts` entry, no `doctor` check, no uniqueness contract. The only tool change\nis a general `rp search` over titles, which is justified independently.\n\n## Why the title, and not a field\n\nRejected, in the order they were considered:\n\n- **A dedicated `aliases: [...]` field.** Schema change, a new merge surface, and a new\n  uniqueness contract for `doctor` to police — all of it unnecessary once titles carry the\n  handle and search can find it.\n- **An alias as a label.** Decisive objection: the GitHub provider maps `labels`\n  bidirectionally (`providers/github.py:104-118`), so an alias label would sync to GitHub, be\n  editable there, and fight the set-wise merge.\n- **A repo-global prefix→id store.** Duplicate state — the ticket knows its handle and so\n  does the file, so `doctor` gains a drift check that can fail — plus a brand-new git conflict\n  surface unless it is append-only JSONL under `merge=union`. It also buys nothing for\n  allocation, since \"next free number\" is a filter over already-folded tickets.\n\n## Why clashes are safe\n\nTwo branches independently allocating `[addr-7]` is **not a corruption**. The ids stay\ndistinct and correct; two tickets simply have titles containing the same text. `rp` has no\nuniqueness contract over titles to violate, so nothing to detect and nothing to repair at the\ntool level. Search returns both rows; renumbering one is the fix.\n\nThis is the whole reason the handle must not be an identity. It is also why the clash window\nis small in practice: a map's tickets are allocated serially in one charting session, so only\nlater additions can collide.\n\n## Why substring search is exact\n\nThe brackets delimit, so no special matching logic is needed: `[addr-2]` is **not** a\nsubstring of `[addr-20]` — the closing bracket differs. Plain substring search over titles is\ntherefore exact for handles, which keeps `rp search` dumb.\n\n## Costs accepted\n\n- **One handle per ticket.** A plural alias list would have allowed several; a second handle\n  in a title pollutes it. Conscious YAGNI trade.\n- **Titles are mutable and sync bidirectionally** under per-field LWW (§8.2), so an edit on a\n  remote can win and silently drop the handle. A local-only field would have been immune.\n  Bounded here: syncing is a deliberate maintainer action, not ordinary skill work, and map\n  tickets are local planning artifacts. Must be documented rather than defended against.\n\n## Corroboration\n\nThe prior-art survey ([addr-2]) found that **no** surveyed tool treats a non-id string as an\nidentity — title lookup is always a separate, list-returning search (`sd search` in seeds;\n`git :/regex` makes its multiple-match rule explicit and positional). This decision was\nreached independently and lands in the same place."}
{"id":"01M0FKAGVPFFM7P22080Z0W5AZ","ts":"2026-08-20T12:46:19.510Z","ticket":"keswrj","op":"set","actor":"runner/copilot@wayfinder","set":{"status":"done"},"reason":"No: titles are search keys, never identities. Human handles ride in the title as a leading bracketed prefix ([addr-2] ...); rp learns nothing but gains a general search."}
{"id":"01M0FKBTDA4CW9E4X41KSHD7MS","ts":"2026-08-20T12:47:02.058Z","ticket":"48t3eh","op":"create","actor":"runner/copilot@wayfinder","set":{"title":"[addr-7] Specify rp search","type":"task","status":"open","priority":1,"parent":"3er9f4","labels+":["wayfinder:grilling"],"body":"## Question\n\nWhat exactly does `rp search` cover, and what does it return?\n\nThe decision in [addr-5] made search the *only* tool change needed for human handles, which\nputs weight on getting its surface right. No search exists today: `rp list` has fixed filters\n(`--status`, `--label`, `--parent`, `--type`) and no free-text matching.\n\nSettle:\n\n- **Which fields.** Title alone is enough for handles and is the cheapest, most predictable\n  option. Bodies and comments are where prose lives, and searching them is a different\n  feature with a different cost — the fold holds bodies inline today (§13.2), so a body scan\n  is not free. Decide whether this is `--in title,body` or title-only, and resist scope creep\n  toward a general text index.\n- **Match semantics.** Plain case-insensitive substring is enough to make `[addr-2]` exact,\n  because the brackets delimit. Anything fuzzier (regex, token matching, ranking) buys\n  nothing for handles and costs determinism, which the map's standing constraint values above\n  convenience.\n- **Output shape.** Same row shape as `rp list --json`, so agents parse one format. Confirm.\n- **Composability with existing filters.** `rp search \"[addr-\" --parent <map>` is how a\n  wayfinder session would enumerate a map's handles. Does search accept the `list` filters, or\n  is it a separate, unfiltered command?\n- **Exit codes.** Zero matches: `0` with an empty list, or `1`? §10 says non-zero on error,\n  and \"found nothing\" is not an error — but a script distinguishing the two matters. Note\n  `rp list` sets the precedent; follow it rather than inventing.\n- **Whether `list` absorbs it instead.** A `--match <text>` flag on `rp list` is strictly less\n  surface than a new command and inherits every filter for free. This is the KISS option and\n  should be beaten, not assumed away.\n\n**Resolved when** the surface is specified precisely enough to implement and to write into\n§10: fields covered, match semantics, output shape, filter composition, and exit codes —\nincluding the possible answer that this is a flag on `rp list` rather than a command."}}
{"id":"01M0FKBTGQ1W9Y86TC5YPB6AED","ts":"2026-08-20T12:47:02.167Z","ticket":"39t0vw","op":"create","actor":"runner/copilot@wayfinder","set":{"title":"[addr-8] What wayfinder does when two handles clash","type":"task","status":"open","priority":2,"parent":"3er9f4","labels+":["wayfinder:grilling"],"body":"## Question\n\nWhat does a wayfinder session do when two tickets carry the same handle?\n\n[addr-5] established that a clash is safe — the ids stay distinct, `rp` has no uniqueness\ncontract over titles, and nothing is corrupt. That makes this purely a **skill behaviour**\nquestion, and the skill has to answer it because nothing else will: no `doctor` check will\never fire, so an unhandled clash simply sits there until it confuses someone.\n\nSettle:\n\n- **Detection.** A session already lists the map's children to find the frontier, so it can\n  spot duplicate handles in the same pass. Is that check worth making explicit in the skill,\n  or does it only run at allocation time (when picking the next free number)?\n- **Allocation under concurrency.** \"Next free number is max+1\" is what a charting session\n  does naturally. Two branches adding a ticket later both take `N+1`. Is anything cheap worth\n  doing here — leaving gaps, or deriving the number from something already unique — or is\n  accepting the rare clash the correct KISS answer?\n- **Repair, and who loses.** Renumbering one side needs a deterministic tiebreak so two\n  agents repairing concurrently reach the same answer. Ticket `created` timestamp is the\n  obvious candidate; ids sort deterministically too. Pick one and say why.\n- **Dangling references.** A renumbered handle may already be written into the map's\n  Decisions-so-far, a commit message, or a PR comment. Map bodies are editable; commit\n  messages are not. Decide whether repair rewrites the references it can reach, and what the\n  skill tells the human about the ones it cannot.\n- **Where it is written down.** The user asked for this to live in the Rohrpost\n  documentation, agent-first: `docs/agents/issue-tracker.md` (Wayfinding operations) and the\n  installable skill in `skills/skills/rohrpost`. Note that an installed copy of the skill can\n  drift from the repo — that staleness problem is out of scope here and sits in the map's fog.\n\n**Resolved when** the skill's clash behaviour is specified — detection point, allocation rule,\ndeterministic tiebreak for repair, and reference handling — and the wording is ready to drop\ninto the tracker doc."}}
{"id":"01M0FKCTC6QY79X0HJB5QPAWMD","ts":"2026-08-20T12:47:34.790Z","ticket":"3er9f4","op":"set","actor":"runner/copilot@wayfinder","set":{"body":"## Destination\n\nA locked decision on **how a human addresses a Rohrpost ticket**, recorded as an ADR plus an\namendment to spec §5.1 (Identifiers) and §10 (CLI), ready to hand to a build session. The\ndecision, not the code: implementation is handed off once the way is clear.\n\n## Notes\n\n**Domain.** Rohrpost's id/addressing ergonomics, driven by `/wayfinder` being awkward to\ndrive by hand: `RP-jdt9c2` is hard to type and impossible to hold in your head.\n\n**Skills.** Call `grilling` and `domain-modeling` every session. Use `research` for facts\noutside this repo. No `CONTEXT.md` exists yet — create one when the first term is resolved.\n\n**Plan, don't do.** The destination is a decision, not shipped code. One explicit exception,\ngranted by the user: `rp search` is authorised to be built once the handle convention has\nbeen dogfooded on this map.\n\n**Handles.** This map dogfoods its own decision ([addr-5]): every ticket carries a leading\nbracketed handle, `[addr-N]`, and the map itself is `[addr]`. Refer to tickets by handle plus\ntitle; the 6-char id stays the identity.\n\n**Standing constraints, settled while charting:**\n\n- **Agent-first holds** (`AGENTS.md`). Human affordances live on the *input* side only — what\n  `rp` accepts. What agents emit and consume does not change; `rp ready --json` stays fast\n  and small (§10).\n- **Ambiguity is a hard error** (`exit 1`), never a guess. Determinism is worth more to an\n  agent than convenience is to a human. *(Confirmed by prior art: every comparable tool does\n  exactly this.)*\n- **The stored id form is fixed** at 6 lowercase base32 chars (§5.1). Only the *addressing\n  layer* is in play — what `rp` accepts as input and what it renders.\n- **\"Hard to type\" and \"hard to reason about\" are two problems.** Typing is input\n  ergonomics. Reasoning is a rendering-and-prose problem: 6 random characters can never be\n  memorable, so the fix is to keep bare ids out of the human's face, not to make them\n  prettier.\n\n## Decisions so far\n\n<!-- one line per closed ticket; zoom the link for the detail it holds -->\n\n- **[addr-2] How comparable tools make random ids addressable** (`RP-twaq4c`): every tool with\n  random ids and human users (jj, git, hg, Sapling, beads) implements **unique-prefix\n  resolution with hard-fail ambiguity**, and **no** tool treats a title as identity — title\n  lookup is always a separate, list-returning search. Write-up:\n  [`docs/research/id-addressing-prior-art.md`](docs/research/id-addressing-prior-art.md).\n- **[addr-5] Should rp resolve a ticket by title?** (`RP-keswrj`): **No**, and no alias field\n  either. Titles are **search keys, never identities**. Human handles ride *in* the title as a\n  leading bracketed prefix — `[addr-2] ...` — with a per-map prefix agreed at charting time.\n  `rp` learns nothing about the convention; the only tool change is a general search. Clashes\n  are safe precisely because the handle is not an identity: two tickets sharing `[addr-7]` is\n  a duplicate string, not a corrupt id.\n\n## Not yet specified\n\n- **The write-up itself**: which ADR, and the exact §5.1 / §10 wording. Waits on the\n  remaining addressing mechanism ([addr-4]) being settled.\n- **Shell completion**: whether it is part of the answer, and for which shells. None exists\n  today, and neither does it in `seeds`. Waits on the moments audit.\n- **A positional handle** over the last result set (`rp show @2` against `rp ready`). Waits\n  on the moments audit and on the rendering question. Note this overlaps [addr-5]'s handles —\n  it may already be redundant.\n- **Doc staleness for installed skills**: `skills/skills/rohrpost` ships with the repo, so an\n  installed copy drifts from the tool it documents. Making `rp` self-documenting would fix it,\n  but that is a different effort from addressing and would widen this map past its\n  destination.\n\n## Out of scope\n\n- **Changing the stored id form** — sequential numbers, counters, or title-derived slugs.\n  Spec §5.1 rejects these deliberately: they need either central coordination (which this\n  design exists to avoid) or mutability. The prior-art research closed this off further:\n  every tool with short sequential keys (GitHub, Linear, Jira) pays for a central allocator,\n  and Mercurial, which allocated locally instead, warns in its own docs that the result must\n  not be used to talk to other people. Reopening this would redraw the destination, and that\n  is a fresh effort, not a resumption.\n- **Handles as a synced field.** Considered and dropped while resolving [addr-5]: an alias\n  carried as a label syncs to GitHub bidirectionally and would be editable there. Handles stay\n  local, inside the title."}}
{"id":"01M0FKS5ZFV6J5S6SAVN6WN6R4","ts":"2026-08-20T12:54:19.887Z","ticket":"48t3eh","op":"set","actor":"runner/copilot@wayfinder","set":{"title":"[addr-7] Specify the search surface"}}
{"id":"01M0FKS62JG86K09VX4TW92S1Q","ts":"2026-08-20T12:54:19.986Z","ticket":"48t3eh","op":"comment","actor":"runner/copilot@wayfinder","text":"**A `--match` filter on `rp list`, not a `rp search` command.** Shipped.\n\n## The surface\n\n```\nrp list --match <text>\n```\n\n- **Fields covered: title only.** Bodies are held inline in the fold (§13.2), so scanning them\n  is not free, and prose search is a different feature with a different cost. Handles and\n  titles are what a human recalls; that is what `--match` covers.\n- **Match semantics: case-insensitive substring.** Nothing fuzzier. Regex, tokenising and\n  ranking buy nothing for handles and cost determinism, which this map values above\n  convenience. Plain substring is already *exact* for handles because the brackets delimit\n  ([addr-5]).\n- **Output shape: unchanged.** `--match` is a filter like any other, so rows are the same\n  shape agents already parse.\n- **Composes with every existing filter**, which is the whole reason it is a flag:\n  `rp list --match \"[addr-\" --parent <map-id> --json` enumerates one map's handled tickets.\n- **Exit code: `0` with an empty result.** Finding nothing is not an error; `rp list` already\n  set that precedent and it was followed rather than re-litigated.\n\n## Why a flag beat a command\n\n`rp search` was the name in play throughout the discussion, but a separate command either\nduplicates the four existing filters or does without them — and a search that cannot be scoped\nto a parent or a status is not much use for finding one map's tickets. The flag inherits all\nof them for free and adds no new command to the CLI surface (§10). KISS/YAGNI, per\n`AGENTS.md`.\n\n## Implementation\n\nOne predicate in `api._list_predicates`, the existing chokepoint every filter already funnels\nthrough, plus the argparse flag and a pass-through in `cmd_list`. No new module, no new\nconcept in the data model.\n\nThree tests in `tests/test_api.py`, one of them a regression test for the bracket-delimiting\nproperty that the entire handle convention rests on: `[addr-2]` must not match `[addr-20]`.\n\nDocs updated: spec §10, `docs/users/README.md`, `docs/agents/issue-tracker.md`, and the\ninstallable skill.\n\nVerified against the live map: `rp list --match \"[addr-2]\"` returns exactly one ticket, and\n`rp list --match \"[addr]\"` returns exactly the map epic."}
{"id":"01M0FKS6566H7QV4HXBVFESP7Y","ts":"2026-08-20T12:54:20.070Z","ticket":"48t3eh","op":"set","actor":"runner/copilot@wayfinder","set":{"status":"done"},"reason":"Shipped as 'rp list --match <text>' (case-insensitive title substring) rather than a separate rp search: a flag inherits every existing filter, a command would duplicate them."}
{"id":"01M0FKSHM1KA1ZMDAYPXAY8BPR","ts":"2026-08-20T12:54:31.809Z","ticket":"3er9f4","op":"set","actor":"runner/copilot@wayfinder","set":{"body":"## Destination\n\nA locked decision on **how a human addresses a Rohrpost ticket**, recorded as an ADR plus an\namendment to spec §5.1 (Identifiers) and §10 (CLI), ready to hand to a build session. The\ndecision, not the code: implementation is handed off once the way is clear.\n\n## Notes\n\n**Domain.** Rohrpost's id/addressing ergonomics, driven by `/wayfinder` being awkward to\ndrive by hand: `RP-jdt9c2` is hard to type and impossible to hold in your head.\n\n**Skills.** Call `grilling` and `domain-modeling` every session. Use `research` for facts\noutside this repo. No `CONTEXT.md` exists yet — create one when the first term is resolved.\n\n**Plan, don't do.** The destination is a decision, not shipped code. One exception was\ngranted and is now spent: the search surface was built ([addr-7]). Everything else still\nplans.\n\n**Handles.** This map dogfoods its own decision ([addr-5]): every ticket carries a leading\nbracketed handle, `[addr-N]`, and the map itself is `[addr]`. Refer to tickets by handle plus\ntitle; the 6-char id stays the identity.\n\n**Standing constraints, settled while charting:**\n\n- **Agent-first holds** (`AGENTS.md`). Human affordances live on the *input* side only — what\n  `rp` accepts. What agents emit and consume does not change; `rp ready --json` stays fast\n  and small (§10).\n- **Ambiguity is a hard error** (`exit 1`), never a guess. Determinism is worth more to an\n  agent than convenience is to a human. *(Confirmed by prior art: every comparable tool does\n  exactly this.)*\n- **The stored id form is fixed** at 6 lowercase base32 chars (§5.1). Only the *addressing\n  layer* is in play — what `rp` accepts as input and what it renders.\n- **\"Hard to type\" and \"hard to reason about\" are two problems.** Typing is input\n  ergonomics. Reasoning is a rendering-and-prose problem: 6 random characters can never be\n  memorable, so the fix is to keep bare ids out of the human's face, not to make them\n  prettier.\n\n## Decisions so far\n\n<!-- one line per closed ticket; zoom the link for the detail it holds -->\n\n- **[addr-2] How comparable tools make random ids addressable** (`RP-twaq4c`): every tool with\n  random ids and human users (jj, git, hg, Sapling, beads) implements **unique-prefix\n  resolution with hard-fail ambiguity**, and **no** tool treats a title as identity — title\n  lookup is always a separate, list-returning search. Write-up:\n  [`docs/research/id-addressing-prior-art.md`](docs/research/id-addressing-prior-art.md).\n- **[addr-5] Should rp resolve a ticket by title?** (`RP-keswrj`): **No**, and no alias field\n  either. Titles are **search keys, never identities**. Human handles ride *in* the title as a\n  leading bracketed prefix — `[addr-2] ...` — with a per-map prefix agreed at charting time.\n  `rp` learns nothing about the convention; the only tool change is a general search. Clashes\n  are safe precisely because the handle is not an identity: two tickets sharing `[addr-7]` is\n  a duplicate string, not a corrupt id.\n\n- **[addr-7] Specify the search surface** (`RP-48t3eh`): shipped as **`rp list --match\n  <text>`**, a case-insensitive substring of the *title*, rather than a separate `rp search`.\n  A flag inherits every existing filter (`--parent`, `--status`, `--label`, `--type`); a\n  command would duplicate them or do without. Bodies are deliberately not searched.\n\n## Not yet specified\n\n- **The write-up itself**: which ADR, and the exact §5.1 / §10 wording. Waits on the\n  remaining addressing mechanism ([addr-4]) being settled.\n- **Shell completion**: whether it is part of the answer, and for which shells. None exists\n  today, and neither does it in `seeds`. Waits on the moments audit.\n- **Doc staleness for installed skills**: `skills/skills/rohrpost` ships with the repo, so an\n  installed copy drifts from the tool it documents. Making `rp` self-documenting would fix it,\n  but that is a different effort from addressing and would widen this map past its\n  destination.\n\n## Out of scope\n\n- **Changing the stored id form** — sequential numbers, counters, or title-derived slugs.\n  Spec §5.1 rejects these deliberately: they need either central coordination (which this\n  design exists to avoid) or mutability. The prior-art research closed this off further:\n  every tool with short sequential keys (GitHub, Linear, Jira) pays for a central allocator,\n  and Mercurial, which allocated locally instead, warns in its own docs that the result must\n  not be used to talk to other people. Reopening this would redraw the destination, and that\n  is a fresh effort, not a resumption.\n- **Handles as a synced field.** Considered and dropped while resolving [addr-5]: an alias\n  carried as a label syncs to GitHub bidirectionally and would be editable there. Handles stay\n  local, inside the title."}}
{"id":"01M0FKZ7759F273EYE5VSQNJHE","ts":"2026-08-20T12:57:37.765Z","ticket":"39t0vw","op":"set","actor":"runner/copilot@wayfinder","set":{"status":"done"},"reason":"Out of scope: handle allocation and clash repair are the charting skill's business, not rp's. The tool's only stake — a clash is a duplicate string, never a corrupt id — was settled in [addr-5]."}
{"id":"01M0FKZ7AMS6HS4HC2EF83N1JH","ts":"2026-08-20T12:57:37.876Z","ticket":"3er9f4","op":"set","actor":"runner/copilot@wayfinder","set":{"body":"## Destination\n\nA locked decision on **how a human addresses a Rohrpost ticket**, recorded as an ADR plus an\namendment to spec §5.1 (Identifiers) and §10 (CLI), ready to hand to a build session. The\ndecision, not the code: implementation is handed off once the way is clear.\n\n## Notes\n\n**Domain.** Rohrpost's id/addressing ergonomics, driven by `/wayfinder` being awkward to\ndrive by hand: `RP-jdt9c2` is hard to type and impossible to hold in your head.\n\n**Skills.** Call `grilling` and `domain-modeling` every session. Use `research` for facts\noutside this repo. No `CONTEXT.md` exists yet — create one when the first term is resolved.\n\n**Plan, don't do.** The destination is a decision, not shipped code. One exception was\ngranted and is now spent: the search surface was built ([addr-7]). Everything else still\nplans.\n\n**Handles.** This map dogfoods its own decision ([addr-5]): every ticket carries a leading\nbracketed handle, `[addr-N]`, and the map itself is `[addr]`. Refer to tickets by handle plus\ntitle; the 6-char id stays the identity.\n\n**Standing constraints, settled while charting:**\n\n- **Agent-first holds** (`AGENTS.md`). Human affordances live on the *input* side only — what\n  `rp` accepts. What agents emit and consume does not change; `rp ready --json` stays fast\n  and small (§10).\n- **Ambiguity is a hard error** (`exit 1`), never a guess. Determinism is worth more to an\n  agent than convenience is to a human. *(Confirmed by prior art: every comparable tool does\n  exactly this.)*\n- **The stored id form is fixed** at 6 lowercase base32 chars (§5.1). Only the *addressing\n  layer* is in play — what `rp` accepts as input and what it renders.\n- **\"Hard to type\" and \"hard to reason about\" are two problems.** Typing is input\n  ergonomics. Reasoning is a rendering-and-prose problem: 6 random characters can never be\n  memorable, so the fix is to keep bare ids out of the human's face, not to make them\n  prettier.\n\n## Decisions so far\n\n<!-- one line per closed ticket; zoom the link for the detail it holds -->\n\n- **[addr-2] How comparable tools make random ids addressable** (`RP-twaq4c`): every tool with\n  random ids and human users (jj, git, hg, Sapling, beads) implements **unique-prefix\n  resolution with hard-fail ambiguity**, and **no** tool treats a title as identity — title\n  lookup is always a separate, list-returning search. Write-up:\n  [`docs/research/id-addressing-prior-art.md`](docs/research/id-addressing-prior-art.md).\n- **[addr-5] Should rp resolve a ticket by title?** (`RP-keswrj`): **No**, and no alias field\n  either. Titles are **search keys, never identities**. Human handles ride *in* the title as a\n  leading bracketed prefix — `[addr-2] ...` — with a per-map prefix agreed at charting time.\n  `rp` learns nothing about the convention; the only tool change is a general search. Clashes\n  are safe precisely because the handle is not an identity: two tickets sharing `[addr-7]` is\n  a duplicate string, not a corrupt id.\n\n- **[addr-7] Specify the search surface** (`RP-48t3eh`): shipped as **`rp list --match\n  <text>`**, a case-insensitive substring of the *title*, rather than a separate `rp search`.\n  A flag inherits every existing filter (`--parent`, `--status`, `--label`, `--type`); a\n  command would duplicate them or do without. Bodies are deliberately not searched.\n\n## Not yet specified\n\n- **The write-up itself**: which ADR, and the exact §5.1 / §10 wording. Waits on the\n  remaining addressing mechanism ([addr-4]) being settled.\n- **Shell completion**: whether it is part of the answer, and for which shells. None exists\n  today, and neither does it in `seeds`. Waits on the moments audit.\n- **Doc staleness for installed skills**: `skills/skills/rohrpost` ships with the repo, so an\n  installed copy drifts from the tool it documents. Making `rp` self-documenting would fix it,\n  but that is a different effort from addressing and would widen this map past its\n  destination.\n\n## Out of scope\n\n- **Changing the stored id form** — sequential numbers, counters, or title-derived slugs.\n  Spec §5.1 rejects these deliberately: they need either central coordination (which this\n  design exists to avoid) or mutability. The prior-art research closed this off further:\n  every tool with short sequential keys (GitHub, Linear, Jira) pays for a central allocator,\n  and Mercurial, which allocated locally instead, warns in its own docs that the result must\n  not be used to talk to other people. Reopening this would redraw the destination, and that\n  is a fresh effort, not a resumption.\n- **Handles as a synced field.** Considered and dropped while resolving [addr-5]: an alias\n  carried as a label syncs to GitHub bidirectionally and would be editable there. Handles stay\n  local, inside the title.\n- **[addr-8] What wayfinder does when two handles clash** (`RP-39t0vw`, closed unresolved):\n  allocating handles and repairing a clash are the **charting skill's** business, not `rp`'s.\n  This map's destination is a decision about how `rp` addresses tickets, and [addr-5] already\n  established the only property the tool owns — a clash is a duplicate string, never a corrupt\n  id. Specifying the skill's behaviour sits past that boundary and belongs to `/wayfinder`."}}
{"id":"01M0FKZN01R37QFHNZ1JG1VT89","ts":"2026-08-20T12:57:51.873Z","ticket":"39t0vw","op":"set","actor":"runner/copilot@wayfinder","set":{"status":"dropped"},"reason":"Out of scope: handle allocation and clash repair are the charting skill's business, not rp's. The tool's only stake — a clash is a duplicate string, never a corrupt id — was settled in [addr-5]."}
{"id":"01M0CXGXDW6CRAAW7Z49GYWWBG","ts":"2026-08-19T11:46:51.452Z","ticket":"aw1404","op":"create","actor":"user/evilissimo@gmail.com","set":{"title":"Map: absicht web UI and LLM editor","type":"epic","status":"open","priority":2,"labels+":["wayfinder:map"],"body":"## Destination\nCreate a web UI for absicht that lets a user create and edit the design, navigate the C4-like structure, call out broken constraints with obvious red, and chat with an LLM that makes approved design changes on the repo.\n\n## Notes\n- Repo focus: absicht design authoring surface, not a general product backlog.\n- Relevant skills: /grilling, /domain-modeling, /prototype, /research.\n- The UI is a surface over the existing `.absicht/` store and schema, not a second source of truth.\n- Preference: decision-first plan; keep the LLM in a reviewable, guardrailed editing loop.\n\n## Decisions so far\n\n- none yet\n\n## Not yet specified\n- Which parts of the design graph are editable in v1 versus read-only.\n- Whether the LLM writes directly to files or goes through a patch/review workflow.\n- How red highlighting is derived from schema and contract checks versus visual warnings.\n- Which chat actions are allowed to mutate design state and which are read-only.\n\n## Out of scope\n- Building a general-purpose multi-user design platform.\n- Full realtime collaboration or audit trails beyond the first reviewable workflow.\n- A broad productization effort outside the absicht authoring use case."}}
{"id":"01M0CXGXJ5XHV6WKHC4RQ1KJRA","ts":"2026-08-19T11:46:51.589Z","ticket":"0mpp1c","op":"create","actor":"user/evilissimo@gmail.com","set":{"title":"Define the v1 product boundary and user flows for the absicht web UI","type":"task","status":"open","priority":2,"parent":"aw1404","labels+":["wayfinder:grilling"],"body":"## Question\nWhat are the v1 product boundaries, user flows, and editing affordances for the absicht web UI, so the editor is shaped around the actual design tasks instead of a generic canvas?"}}
{"id":"01M0CXGXNEDN9WKZ3EXXMJ0RG2","ts":"2026-08-19T11:46:51.694Z","ticket":"ajqr7a","op":"create","actor":"user/evilissimo@gmail.com","set":{"title":"Inventory the absicht model and schema constraints for the editor","type":"task","status":"open","priority":2,"parent":"aw1404","labels+":["wayfinder:research"],"body":"## Question\nWhat does the current absicht model, schema, and design store already support, and what exact editing operations must the web UI expose to remain faithful to the repo's design truth?"}}
{"id":"01M0CXGXSMKTMB05EW0DYYZT2V","ts":"2026-08-19T11:46:51.828Z","ticket":"2dr7s7","op":"create","actor":"user/evilissimo@gmail.com","set":{"title":"Prototype the C4-like navigation and red-flag semantics","type":"task","status":"open","priority":2,"parent":"aw1404","labels+":["wayfinder:prototype"],"blocked_by+":["0mpp1c"],"body":"## Question\nWhat should the C4-like navigation and red-warning semantics look like in a first working prototype, and how should the interface signal structural issues without turning every violation into noise?"}}
{"id":"01M0CXGXYST9K03EX9DJ5YB376","ts":"2026-08-19T11:46:51.993Z","ticket":"4q8f0a","op":"create","actor":"user/evilissimo@gmail.com","set":{"title":"Define the LLM editing contract and safety boundaries","type":"task","status":"open","priority":2,"parent":"aw1404","labels+":["wayfinder:grilling"],"blocked_by+":["0mpp1c","ajqr7a"],"body":"## Question\nWhat is the safe LLM editing contract for absicht, including which prompts, operations, and review gates can mutate design files versus only suggest changes?"}}
{"id":"01M0CXJ6D16TJJ5HHJDKQSX45G","ts":"2026-08-19T11:47:33.409Z","ticket":"0mpp1c","op":"set","actor":"user/evilissimo@gmail.com","set":{"status":"in_progress","assignee":"user/evilissimo@gmail.com"}}
{"id":"01M1EB0BRD4YCW59DZQNVS9CFE","ts":"2026-09-01T11:17:25.389Z","ticket":"06ywvd","op":"create","actor":"runner/wayfinder@win-chart","set":{"title":"[win] Windows-native rohrpost: rp + skills without WSL","type":"epic","status":"open","priority":1,"labels+":["wayfinder:map"],"body":"## Destination\n\n`rp` runs natively on Windows (CPython 3.14 via uv/uvx), the test suite is green on a `windows-latest` CI leg, and every `rp`-touching skill works from PowerShell, cmd, and Git Bash — no WSL. POSIX behaviour unchanged.\n\n## Notes\n\n- Plan-don't-do is overridden for the `wayfinder:task` tickets: execution is carried inside this map; the destination is a delivered change.\n- Work each task ticket in its own branch/worktree; commit every green increment; merge to main once green.\n- Windows install root for the local wrapper defaults to `%LOCALAPPDATA%\\rohrpost`; `ROHRPOST_HOME` keeps precedence.\n- Settled decisions shaping every ticket: all three shells in scope (PowerShell/cmd/Git Bash); `--body-file` replaces heredoc teaching; skill docs = inline Windows sections + one shared Windows playbook; spec §7 rewritten to the invariant (mechanism as implementation note); locking = platform split (`msvcrt`/`fcntl`), no new dependencies.\n- `rp`-touching surfaces in scope: the `rohrpost` skill, `setup-repo-skills` (its Rohrpost tracker template), `babysit-pr`, and this repo's `docs/agents/issue-tracker.md`.\n\n## Decisions so far\n\n<!-- one line per closed ticket: [title](link): gist -->\n\n## Not yet specified\n\n- Whatever the Windows CI leg exposes beyond the locking fix — temp-file, permission-bit, or path assumptions elsewhere in `src/`. Cannot be phrased sharply until [win-9] has run.\n\n## Out of scope\n\n- Developing rohrpost itself on Windows (Makefile targets, mutation runs stay POSIX-first).\n- Standalone `.exe` distribution (uvx / local wrapper is the install path).\n- WSL as a support target."}}
{"id":"01M1EB1MECCZKQN3T13RVZY0XW","ts":"2026-09-01T11:18:07.052Z","ticket":"acx7sx","op":"create","actor":"runner/wayfinder@win-chart","set":{"title":"[win-1] research: msvcrt locking semantics for a blocking exclusive lock","type":"task","status":"open","priority":1,"parent":"06ywvd","labels+":["wayfinder:research"],"body":"## Question\n\n`store.file_lock` is today `fcntl.flock` (exclusive, blocking, per open file description, callers must not nest). What does `msvcrt.locking` actually provide on Windows, and what must a blocking exclusive lock on `.rohrpost/.lock` look like in CPython 3.14?\n\nPin down, each claim to a primary source (Python docs, CPython source, Microsoft docs):\n\n- lock granularity: byte-range on the `.lock` file — which range, and does the file need nonzero length?\n- lock scope: per file handle vs per process; what happens on a second handle in the same process?\n- `LK_LOCK` retry behaviour (tries, interval) vs `LK_NBLCK`/`LK_RLCK`; unlock (`LK_UNLCK`) requirements\n- blocking loop shape: is `LK_LOCK`'s built-in retry sufficient, or spin on `LK_NBLCK` with backoff?\n- crash behaviour: are locks released when the holding process dies?\n- interaction with `lock.open(\"a+\", encoding=\"utf-8\")` (text mode, append) and `msvcrt.locking`'s fd requirements\n\nResolved by the `research` skill: findings to `docs/research/windows-file-locking.md`, every claim carrying its source link."}}
{"id":"01M1EB1MK984QRG888P4H213PD","ts":"2026-09-01T11:18:07.209Z","ticket":"5t88jp","op":"create","actor":"runner/wayfinder@win-chart","set":{"title":"[win-2] research: uv + Python 3.14 on Windows runtime facts","type":"task","status":"open","priority":1,"parent":"06ywvd","labels+":["wayfinder:research"],"body":"## Question\n\nWhich runtime facts about uv and CPython on native Windows do the wrapper, playbook, and CI work depend on? Primary sources only (uv docs at astral.sh/docs, Python on Windows docs, GitHub uv releases):\n\n- `.venv` layout after `uv sync` on Windows: where does the `rp` console script land (`Scripts/rp.exe`? `rp-script.py`?), and how do the shims behave from PowerShell, cmd, and Git Bash?\n- `uvx rohrpost` on Windows: resolution, cache location, first-run latency characteristics\n- install-root convention: is `%LOCALAPPDATA%\\<app>` the idiomatic cache-style home (vs `%APPDATA%`, `%USERPROFILE%`)?\n- gotchas: WindowsApps `python.exe` execution aliases, MAX_PATH/long-path behaviour for deep `.venv` trees, OneDrive/defender interference worth warning about\n\nResolved by the `research` skill: findings to `docs/research/windows-uv-runtime.md`, every claim carrying its source link."}}
{"id":"01M1EB1MRXBMS5JD50GGJDKPY9","ts":"2026-09-01T11:18:07.389Z","ticket":"c9tb4p","op":"create","actor":"runner/wayfinder@win-chart","set":{"title":"[win-3] research: Git for Windows vs a committed JSONL event log","type":"task","status":"open","priority":1,"parent":"06ywvd","labels+":["wayfinder:research"],"body":"## Question\n\nWhich Git-for-Windows behaviours threaten a committed JSONL event log (`.rohrpost/log.jsonl`) or the sync merge path, and which `.gitattributes`/config choices fix them? Primary sources only (git-scm docs, gitforwindows.org, Git source where needed):\n\n- `core.autocrlf` defaults across Git-for-Windows installer choices; what a CRLF working tree does to our `open(..., \"r\")`/`strip()` reads and to `os.write` appends\n- effect of `text eol=lf` attributes on `.rohrpost/**/*.jsonl`: round-trip identity for a repo checked out on both OSes; do the existing `merge=union` rules interact with CRLF?\n- is `git merge-file` present in typical Git-for-Windows installs (incl. minimal/PortableGit)? `core.longpaths` default and impact\n\nResolved by the `research` skill: findings to `docs/research/windows-git-jsonl.md`, every claim carrying its source link."}}
{"id":"01M1EB1MYAH6Y4579RSETMP350","ts":"2026-09-01T11:18:07.562Z","ticket":"sdjcft","op":"create","actor":"runner/wayfinder@win-chart","set":{"title":"[win-4] Windows locking design for file_lock","type":"task","status":"open","priority":1,"parent":"06ywvd","labels+":["wayfinder:grilling"],"body":"## Question\n\nSettle the design [win-1] informs, precisely enough that [win-7] implements it verbatim:\n\n- which byte range of `.lock` gets locked, and the lock-file lifecycle (created once, never deleted?)\n- blocking: `LK_LOCK`'s built-in retry vs a spin on `LK_NBLCK` with backoff; total wait and failure mode\n- re-entrancy: the docstring's \"callers must not nest\" contract — still true under `msvcrt`?\n- what `doctor` says about a stale `.lock`\n- the POSIX/Windows seam in `store.py`: guarded top-level import vs per-platform helper modules; how tests fake/verify each side"}}
{"id":"01M1EB1N35QY4CJ0XKJ00JDWF2","ts":"2026-09-01T11:18:07.717Z","ticket":"k0xjdm","op":"create","actor":"runner/wayfinder@win-chart","set":{"title":"[win-5] --body-file CLI contract","type":"task","status":"open","priority":1,"parent":"06ywvd","labels+":["wayfinder:grilling"],"body":"## Question\n\nHerdedoc-free multi-line bodies for every shell. Settle the contract [win-8] implements:\n\n- which commands grow `--body-file` (`new` certainly; `comment`? `set` body? `resolve`?)\n- `-` reads stdin; interaction/precedence with `--body` and `--template` (error or last-wins?)\n- error modes: missing file, empty file, undecodable bytes — exit code and message shape\n- docs to touch (`docs/users`, SKILL.md examples) so the heredoc teaching gets a portable sibling"}}
{"id":"01M1EB1N86V9PWTNYZYJ1V2R5R","ts":"2026-09-01T11:18:07.878Z","ticket":"9657gk","op":"create","actor":"runner/wayfinder@win-chart","set":{"title":"[win-6] Spec §7 rewrite: invariant over mechanism","type":"task","status":"open","priority":1,"parent":"06ywvd","labels+":["wayfinder:grilling"],"body":"## Question\n\nRewrite spec §7 so the contract is the invariant — appends serialised under an exclusive advisory lock; a partial line never lands; readers tolerate a torn tail — with per-platform mechanisms (fcntl.flock / msvcrt.locking, O_APPEND semantics incl. the Windows story) as implementation notes, not contract. Existing tests must still map 1:1 onto the reworded guarantees. Blocked by [win-1] so the Windows mechanism's real atomicity story is stated truthfully."}}
{"id":"01M1EB1NCQSKW358SNF81NCKBY","ts":"2026-09-01T11:18:08.023Z","ticket":"zwesnq","op":"create","actor":"runner/wayfinder@win-chart","set":{"title":"[win-7] Implement platform-split locking in store.py","type":"task","status":"open","priority":2,"parent":"06ywvd","labels+":["wayfinder:task"],"body":"## Work\n\nPer [win-4]'s decision: replace the top-level `import fcntl` with the platform seam; `msvcrt.locking` path for Windows (byte-range on `.lock`, blocking per design), `fcntl.flock` elsewhere byte-identical. `append_event` untouched. Tests cover the Windows path (CI leg proves it for real) without weakening the POSIX concurrency tests. `pyproject.toml`'s `Operating System :: OS Independent` claim becomes true."}}
{"id":"01M1EB1NH8GBF6FS5X12H70MC4","ts":"2026-09-01T11:18:08.168Z","ticket":"kyb9vq","op":"create","actor":"runner/wayfinder@win-chart","set":{"title":"[win-8] Implement --body-file in the CLI","type":"task","status":"open","priority":2,"parent":"06ywvd","labels+":["wayfinder:task"],"body":"## Work\n\nPer [win-5]'s contract: add `--body-file` (and stdin `-`) to the settled commands with the agreed precedence and error modes; tests for each. Update `docs/users` and the SKILL.md examples so every heredoc teaching gains a portable sibling."}}
{"id":"01M1EB1NNS27RFY2JDBZXB1D51","ts":"2026-09-01T11:18:08.313Z","ticket":"zy1th8","op":"create","actor":"runner/wayfinder@win-chart","set":{"title":"[win-9] CI windows-latest test leg","type":"task","status":"open","priority":2,"parent":"06ywvd","labels+":["wayfinder:task"],"body":"## Work\n\nCI `test` job gains `windows-latest` (lint/types/security/complexity stay ubuntu). Fix whatever the leg exposes in `src/` beyond the locking seam — temp files, permission bits, path shapes — or graduate surprises into new tickets and update the map's Not-yet-specified. Green on windows-latest is the exit criterion."}}
{"id":"01M1EB1NVYFVPT12H1J72P558C","ts":"2026-09-01T11:18:08.510Z","ticket":"x3f018","op":"create","actor":"runner/wayfinder@win-chart","set":{"title":"[win-10] eol=lf protection for .rohrpost JSONL","type":"task","status":"open","priority":2,"parent":"06ywvd","labels+":["wayfinder:task"],"body":"## Work\n\nPer [win-3]'s findings: `paths.GITATTRIBUTES_RULES` gains `eol=lf` (or `text eol=lf`) for `.rohrpost/**/*.jsonl`; the shipped repo's own `.gitattributes` gets the same rules; `rp init` writes them idempotently as today. Decide (small, inline) whether `doctor` warns when an existing repo lacks the rule. Verify round-trip: a repo checked out on Windows with `autocrlf=true` folds identically."}}
{"id":"01M1EB1P10CNN4YDF1ZSKNVZFF","ts":"2026-09-01T11:18:08.672Z","ticket":"rf1841","op":"create","actor":"runner/wayfinder@win-chart","set":{"title":"[win-11] rohrpost skill Windows pass: wrappers + playbook","type":"task","status":"open","priority":2,"parent":"06ywvd","labels+":["wayfinder:task"],"body":"## Work\n\n- `.ps1` and `.cmd` wrapper templates mirroring `rohrpost.template`: validate install, resolve `.venv/Scripts/rp(.exe)`, default home `%LOCALAPPDATA%\\rohrpost`, honour `ROHRPOST_HOME`; never bypass the wrapper.\n- SKILL.md gains a compact Windows section at the point of use: wrapper resolution per shell, here-string + `--body-file -` pattern replacing heredoc teaching.\n- New shared Windows playbook (install: uv, clone, `uv sync`, wrapper materialisation, validation); `playbooks/install-local.md` points to it for Windows instead of duplicating.\n- Wrapper self-test: extend `test-rohrpost-wrapper.sh` for Git Bash only if cheap; otherwise note it."}}
{"id":"01M1EB1P68SBSPTKA27BXFDHE4","ts":"2026-09-01T11:18:08.841Z","ticket":"4hvpra","op":"create","actor":"runner/wayfinder@win-chart","set":{"title":"[win-12] Windows pass: babysit-pr, setup template, repo tracker doc","type":"task","status":"open","priority":2,"parent":"06ywvd","labels+":["wayfinder:task"],"body":"## Work\n\nFollowing the patterns [win-11] established: update `babysit-pr/SKILL.md`, `setup-repo-skills/issue-tracker-rohrpost.md` (teach `--body-file`/here-strings instead of `$(cat body.md)`), and this repo's `docs/agents/issue-tracker.md` so every `rp` example is executable from PowerShell, cmd, and Git Bash."}}
{"id":"01M1EB1PAK9TJ290DYKEZ43P08","ts":"2026-09-01T11:18:08.979Z","ticket":"sdjcft","op":"set","actor":"runner/wayfinder@win-chart","set":{"blocked_by+":["acx7sx"]}}
{"id":"01M1EB1PF3TZCNF77D3WQH4TH9","ts":"2026-09-01T11:18:09.123Z","ticket":"9657gk","op":"set","actor":"runner/wayfinder@win-chart","set":{"blocked_by+":["acx7sx"]}}
{"id":"01M1EB1PKJ06GKRW9EFENNTJMW","ts":"2026-09-01T11:18:09.266Z","ticket":"zwesnq","op":"set","actor":"runner/wayfinder@win-chart","set":{"blocked_by+":["sdjcft"]}}
{"id":"01M1EB1PQNYE30J83E0BZGAP75","ts":"2026-09-01T11:18:09.397Z","ticket":"kyb9vq","op":"set","actor":"runner/wayfinder@win-chart","set":{"blocked_by+":["k0xjdm"]}}
{"id":"01M1EB1PVY4YRN270ZHGBJ882R","ts":"2026-09-01T11:18:09.534Z","ticket":"zy1th8","op":"set","actor":"runner/wayfinder@win-chart","set":{"blocked_by+":["zwesnq"]}}
{"id":"01M1EB1Q13VTTTVJTB1Z0YY0F7","ts":"2026-09-01T11:18:09.699Z","ticket":"x3f018","op":"set","actor":"runner/wayfinder@win-chart","set":{"blocked_by+":["c9tb4p"]}}
{"id":"01M1EB1Q7G3Z4EKVKQY759TY9B","ts":"2026-09-01T11:18:09.904Z","ticket":"rf1841","op":"set","actor":"runner/wayfinder@win-chart","set":{"blocked_by+":["5t88jp"]}}
{"id":"01M1EB1QD7PPSYGB40Q9SDSZ9V","ts":"2026-09-01T11:18:10.087Z","ticket":"rf1841","op":"set","actor":"runner/wayfinder@win-chart","set":{"blocked_by+":["k0xjdm"]}}
{"id":"01M1EB1QJRWQESENXTF600BKJV","ts":"2026-09-01T11:18:10.264Z","ticket":"4hvpra","op":"set","actor":"runner/wayfinder@win-chart","set":{"blocked_by+":["rf1841"]}}
{"id":"01M1EB3XWM04W1RTM5JANFMMKT","ts":"2026-09-01T11:19:22.260Z","ticket":"acx7sx","op":"set","actor":"runner/res-msvcrt","set":{"status":"in_progress","assignee":"runner/res-msvcrt"}}
{"id":"01M1EB3Y982S62WCXHV2RN6845","ts":"2026-09-01T11:19:22.664Z","ticket":"5t88jp","op":"set","actor":"runner/res-uvwin","set":{"status":"in_progress","assignee":"runner/res-uvwin"}}
{"id":"01M1EB3YQBFXN2HWZXD8T21X15","ts":"2026-09-01T11:19:23.115Z","ticket":"c9tb4p","op":"set","actor":"runner/res-gitwin","set":{"status":"in_progress","assignee":"runner/res-gitwin"}}
{"id":"01M1EBRT3K06KZHJ3R6X91TYY1","ts":"2026-09-01T11:30:46.515Z","ticket":"acx7sx","op":"comment","actor":"runner/res-msvcrt","text":"Researched against primary sources: Python 3.14 msvcrt docs, CPython 3.14 source, MS CRT + Win32 docs, Raymond Chen. Full detail in docs/research/windows-file-locking.md.\n- msvcrt.locking locks a byte range from the fd's current position; ranges may extend past EOF, so a 0-byte .lock file locks fine, the bytes need not exist.\n- Locks attach to the file handle, not the process: a second handle in the same process can neither take an overlapping lock nor read/write the locked bytes. Unlike advisory flock, Windows range locks are enforced (ReadFile/WriteFile fail through other handles).\n- LK_LOCK/LK_RLCK retry once per second, give up after 10 tries (~10s) with OSError/EDEADLOCK; LK_NBLCK fails immediately; LK_UNLCK must name the exact previously locked range (no merging of adjacent regions).\n- No msvcrt mode waits unboundedly: blocking means a Python loop over LK_NBLCK/LK_LOCK; kernel-level block-until-acquired exists only in LockFileEx, which msvcrt does not expose. The GIL is released during locking() calls.\n- The OS unlocks ranges when the holding process dies, but with no timing guarantee; the docs warn access may be denied until the OS completes the unlock.\n- Windows documents _O_APPEND as move-to-end-before-every-write, not as an atomic step; there is no stated equivalent of Linux's single-O_APPEND-write guarantee (spec §7). FILE_APPEND_DATA is an access right, WriteFile's only atomicity claim is single-sector; the documented Windows tool for concurrent appenders is a byte-range lock.\nEvidence only; the decision belongs to RP-sdjcft."}
{"id":"01M1EBWP6R61BCWYVKW311KCS3","ts":"2026-09-01T11:32:53.592Z","ticket":"5t88jp","op":"comment","actor":"runner/res-uvwin","text":"Researched against primary sources; findings in docs/research/windows-uv-runtime.md, every claim sourced.\n\n1. Console script: uv sync puts rp at .venv\\Scripts\\rp.exe — a uv \"trampoline\" PE binary embedding the entry-point zip + python path (no rp-script.py, no embedded Python). Same .exe runs from PowerShell, cmd.exe, Git Bash; Git Bash (Cygwin-lineage) also accepts extensionless rp, with documented script-beats-exe precedence caveat. Activate.ps1 in PowerShell may need execution policy relaxed.\n2. uvx rohrpost: disposable env inside %LOCALAPPDATA%\\uv\\cache; first run resolves latest + builds env, later runs reuse cached version until cache prune/refresh or explicit @version. uv tool install instead uses %APPDATA%\\uv\\data\\tools + copies executables to %USERPROFILE%\\.local\\bin.\n3. Install root: MS docs define LOCALAPPDATA as per-user non-roaming data home, APPDATA as roaming, and forbid app files at %USERPROFILE% root; pip and uv caches both sit in %LOCALAPPDATA%\\<tool>\\cache.\n4. Gotchas: WindowsApps python.exe Store-stub aliases (reparse points, last on user PATH), MAX_PATH 260 opt-in fix (LongPathsEnabled + longPathAware manifest), OneDrive hardlink failures (uv#7906/#9500/#19616; OneDrive rejects symlinks/junctions), Defender real-time scan overhead on unsigned binaries (Dev Drive performance mode exists as MS's own mitigation)."}
{"id":"01M1EC1CB93YC8BZ4478G8XFZS","ts":"2026-09-01T11:35:27.337Z","ticket":"c9tb4p","op":"comment","actor":"runner/res-gitwin","text":"Findings in docs/research/windows-git-jsonl.md (primary sources only).\n\n- Installer default is core.autocrlf=true (checkout CRLF / commit LF) written to the system config; input and false are the other radio choices, upgrades replay prior choice.\n- With autocrlf=true an LF blob checks out as CRLF. rp's open(\"r\")/strip() reads tolerate that; its os.write append does not: the fd lacks O_BINARY, so the CRT text mode translates every appended \\n to \\r\\n on disk (Windows only), and the short-write rollback can't detect it.\n- text eol=lf on .rohrpost/**/*.jsonl normalises to LF in the index on every checkin and checks out blob-identical bytes on any OS, overriding autocrlf/core.eol: byte-identical round trip on Linux and Windows.\n- merge=union does no eol conversion itself; it merges blobs (LF when text applies at checkin). Input renormalisation only with merge.renormalize (off by default). Line matching is byte-exact, so LF-vs-CRLF sides yield duplicated/mixed lines; NUL in first 8000 bytes silently degrades union to 'ours'.\n- git merge-file is a builtin in git.exe: present in installer, PortableGit, and MinGit (--union supported).\n- core.longpaths is GfW-only, disabled by default; enables >260-char (MAX_PATH) paths for builtin commands. Deep .venv trees under a long clone path are how a checkout crosses 260."}
{"id":"01M1EC4RCB6MAEPDQ6EWQF2B2X","ts":"2026-09-01T11:37:17.963Z","ticket":"acx7sx","op":"set","actor":"runner/wayfinder@win-chart","set":{"status":"done"},"reason":"msvcrt.locking: per-handle byte-range lock (works on empty .lock); LK_LOCK only retries 10x1s so blocking needs a Python loop; OS releases on death; no documented single-O_APPEND-write atomicity on Windows"}
{"id":"01M1EC4RJ2XXAP1T3MY9RGX26B","ts":"2026-09-01T11:37:18.146Z","ticket":"5t88jp","op":"set","actor":"runner/wayfinder@win-chart","set":{"status":"done"},"reason":"rp.exe trampoline shim works from PS/cmd/Git Bash; uvx caches under %LOCALAPPDATA%\\uv; %LOCALAPPDATA% is the per-user home; gotchas: Store aliases, MAX_PATH, OneDrive/Defender"}
{"id":"01M1EC4RPGGNM6R0BHF9BQVZHB","ts":"2026-09-01T11:37:18.288Z","ticket":"c9tb4p","op":"set","actor":"runner/wayfinder@win-chart","set":{"status":"done"},"reason":"GfW defaults autocrlf=true; text eol=lf gives byte-identical checkouts; merge=union safe on normalized blobs; merge-file is a builtin everywhere; surprise: text-mode fd rewrites os.write appends"}
{"id":"01M1EC4RVXKB0D9H0R38DE8VTT","ts":"2026-09-01T11:37:18.461Z","ticket":"p9a9dj","op":"create","actor":"runner/wayfinder@win-chart","set":{"title":"[win-13] O_BINARY appends: Windows text-mode fd translation breaks the append contract","type":"task","status":"open","priority":2,"parent":"06ywvd","labels+":["wayfinder:task"],"body":"## Work\n\n[win-3] research found (docs/research/windows-git-jsonl.md): on Windows, `os.open` without `O_BINARY` inherits the CRT's text mode, so each appended `\\n` lands as `\\r\\n` on disk. That breaks `append_event`'s byte-count math (the short-write rollback computes offsets from `len(line)`) and violates §7's one-write-one-line invariant at the byte level.\n\nFix: pass `os.O_BINARY` (Windows-only flag; guard or getattr) in `store.append_event`; test the exact bytes of an appended line on both platforms."}}
{"id":"01M1EC4S2J0MXY221P9WQF6VY7","ts":"2026-09-01T11:37:18.674Z","ticket":"zy1th8","op":"set","actor":"runner/wayfinder@win-chart","set":{"blocked_by+":["p9a9dj"]}}
{"id":"01M1EC4SA3DFN68XJX0DGAE8QZ","ts":"2026-09-01T11:37:18.915Z","ticket":"6zresr","op":"create","actor":"runner/wayfinder@win-chart","set":{"title":"Markdown lint + link checker for docs/ and skills","type":"task","status":"open","priority":3,"labels+":["needs-triage"],"body":"## Context\n\nSurfaced during [win-1..3] research: docs/research notes quote sources in both body and source-index tables, and skills/docs carry many inline links, but no hook checks either.\n\n## Suggestion\n\nAdd mdformat or markdownlint and lychee (link check) to pre-commit, scoped to *.md. Maintainer call."}}
{"id":"01M1EC4SSHN75G9XG6G16H3VWY","ts":"2026-09-01T11:37:19.409Z","ticket":"06ywvd","op":"set","actor":"runner/wayfinder@win-chart","set":{"body":"## Destination\n\n`rp` runs natively on Windows (CPython 3.14 via uv/uvx), the test suite is green on a `windows-latest` CI leg, and every `rp`-touching skill works from PowerShell, cmd, and Git Bash — no WSL. POSIX behaviour unchanged.\n\n## Notes\n\n- Plan-don't-do is overridden for the `wayfinder:task` tickets: execution is carried inside this map; the destination is a delivered change.\n- Work each task ticket in its own branch/worktree; commit every green increment; merge to main once green.\n- Windows install root for the local wrapper defaults to `%LOCALAPPDATA%\\rohrpost`; `ROHRPOST_HOME` keeps precedence.\n- Settled decisions shaping every ticket: all three shells in scope (PowerShell/cmd/Git Bash); `--body-file` replaces heredoc teaching; skill docs = inline Windows sections + one shared Windows playbook; spec §7 rewritten to the invariant (mechanism as implementation note); locking = platform split (`msvcrt`/`fcntl`), no new dependencies.\n- `rp`-touching surfaces in scope: the `rohrpost` skill, `setup-repo-skills` (its Rohrpost tracker template), `babysit-pr`, and this repo's `docs/agents/issue-tracker.md`.\n\n## Decisions so far\n\n- [research: msvcrt locking semantics](RP-acx7sx): per-handle byte-range lock works on an empty `.lock`; `LK_LOCK` is 10x1s-retry only, so blocking needs a Python loop; OS releases locks on death; no documented single-`O_APPEND`-write atomicity on Windows.\n- [research: uv + Python 3.14 on Windows runtime facts](RP-5t88jp): `Scripts/rp.exe` trampoline works from PS/cmd/Git Bash; `uvx` caches under `%LOCALAPPDATA%\\uv`; `%LOCALAPPDATA%` confirmed as per-user home; gotchas: Store python aliases, MAX_PATH, OneDrive/Defender.\n- [research: Git for Windows vs a committed JSONL event log](RP-c9tb4p): GfW defaults `autocrlf=true`; `text eol=lf` gives byte-identical cross-OS checkouts; `merge=union` safe on normalized blobs; `merge-file` is a builtin everywhere. Surprise: text-mode fd translation rewrites appends — graduated early as [win-13].\n\n## Not yet specified\n\n- Whatever the Windows CI leg exposes beyond the locking fix — temp-file, permission-bit, or path assumptions elsewhere in `src/`. Cannot be phrased sharply until [win-9] has run.\n\n## Out of scope\n\n- Developing rohrpost itself on Windows (Makefile targets, mutation runs stay POSIX-first).\n- Standalone `.exe` distribution (uvx / local wrapper is the install path).\n- WSL as a support target."}}
{"id":"01M1EKRWK83MXXD7X4TNB35K4K","ts":"2026-09-01T13:50:37.672Z","ticket":"sdjcft","op":"set","actor":"runner/wayfinder@win-4","set":{"status":"in_progress","assignee":"runner/wayfinder@win-4"}}
{"id":"01M1EM887WJ7ZR0VDWZXHYJQC6","ts":"2026-09-01T13:59:01.116Z","ticket":"sdjcft","op":"comment","actor":"runner/wayfinder@win-4","text":"## Resolution\n\nEvidence base: docs/research/windows-file-locking.md ([win-1]).\n\n1. **Lock target**: `fh.seek(0)`, exclusive lock on byte range [0, 1) of `.lock`. The range may sit past EOF of an empty file (documented as supported). `.lock` is created on first mutation and never deleted — file existence is not lock state.\n2. **Blocking**: `LK_LOCK` as-is — CRT paces retries 1s apart, ~10 attempts, then `OSError(EDEADLOCK)` → `StoreError` naming the dir. No custom spin loop. The bounded wait is a Windows wait characteristic, not contract: §7 keeps the invariant (appends serialised under an exclusive advisory lock), and win-6 should word it that way.\n3. **Re-entrancy**: contract unchanged — callers must not nest two `file_lock` calls on the same dir. Docstring sharpened: nesting deadlocks on POSIX (independent OFD) and fails after ≤10s on Windows (overlap rule); Windows locks are enforced — plain I/O through a second handle fails too.\n4. **doctor**: unchanged and silent on `.lock`.\n5. **Seam**: module-top platform-conditional import (`sys.platform == \"win32\"` → `msvcrt`, else `fcntl`) with module-private `_acquire`/`_release`; `file_lock` keeps its shape; adds `seek(0)`. Windows behaviour is proven by the CI leg's real concurrency tests (win-9) — no Linux-side msvcrt fakes.\n\nConfidence at decision time: Q1 92%, Q2 82%, Q3 95%, Q4 93%, Q5 88%."}
{"id":"01M1EM88F0MYJK2JVYETWQ0SK7","ts":"2026-09-01T13:59:01.345Z","ticket":"sdjcft","op":"set","actor":"runner/wayfinder@win-4","set":{"status":"done"},"reason":"lock 1 byte @0 of a never-deleted .lock; LK_LOCK bounded wait -> loud StoreError; no-nest kept; doctor silent; platform-guarded import seam"}
{"id":"01M1EM88YE0DHRW6BHC6MSDMVK","ts":"2026-09-01T13:59:01.838Z","ticket":"06ywvd","op":"set","actor":"runner/wayfinder@win-4","set":{"body":"## Destination\n\n`rp` runs natively on Windows (CPython 3.14 via uv/uvx), the test suite is green on a `windows-latest` CI leg, and every `rp`-touching skill works from PowerShell, cmd, and Git Bash — no WSL. POSIX behaviour unchanged.\n\n## Notes\n\n- Plan-don't-do is overridden for the `wayfinder:task` tickets: execution is carried inside this map; the destination is a delivered change.\n- Work each task ticket in its own branch/worktree; commit every green increment; merge to main once green.\n- Windows install root for the local wrapper defaults to `%LOCALAPPDATA%\\rohrpost`; `ROHRPOST_HOME` keeps precedence.\n- Settled decisions shaping every ticket: all three shells in scope (PowerShell/cmd/Git Bash); `--body-file` replaces heredoc teaching; skill docs = inline Windows sections + one shared Windows playbook; spec §7 rewritten to the invariant (mechanism as implementation note); locking = platform split (`msvcrt`/`fcntl`), no new dependencies.\n- `rp`-touching surfaces in scope: the `rohrpost` skill, `setup-repo-skills` (its Rohrpost tracker template), `babysit-pr`, and this repo's `docs/agents/issue-tracker.md`.\n\n## Decisions so far\n\n- [research: msvcrt locking semantics](RP-acx7sx): per-handle byte-range lock works on an empty `.lock`; `LK_LOCK` is 10x1s-retry only, so blocking needs a Python loop; OS releases locks on death; no documented single-`O_APPEND`-write atomicity on Windows.\n- [research: uv + Python 3.14 on Windows runtime facts](RP-5t88jp): `Scripts/rp.exe` trampoline works from PS/cmd/Git Bash; `uvx` caches under `%LOCALAPPDATA%\\uv`; `%LOCALAPPDATA%` confirmed as per-user home; gotchas: Store python aliases, MAX_PATH, OneDrive/Defender.\n- [research: Git for Windows vs a committed JSONL event log](RP-c9tb4p): GfW defaults `autocrlf=true`; `text eol=lf` gives byte-identical cross-OS checkouts; `merge=union` safe on normalized blobs; `merge-file` is a builtin everywhere. Surprise: text-mode fd translation rewrites appends — graduated early as [win-13].\n\n- [Windows locking design](RP-sdjcft): lock 1 byte at offset 0 of a never-deleted `.lock`; `LK_LOCK`'s bounded ~10s wait fails loud; no-nest contract kept (fails on both platforms); `doctor` silent; platform-guarded import seam, Windows proven by CI.\n\n## Not yet specified\n\n- Whatever the Windows CI leg exposes beyond the locking fix — temp-file, permission-bit, or path assumptions elsewhere in `src/`. Cannot be phrased sharply until [win-9] has run.\n\n## Out of scope\n\n- Developing rohrpost itself on Windows (Makefile targets, mutation runs stay POSIX-first).\n- Standalone `.exe` distribution (uvx / local wrapper is the install path).\n- WSL as a support target."}}
{"id":"01M1EME43SV5CMD6FBAFAYVQNM","ts":"2026-09-01T14:02:13.497Z","ticket":"k0xjdm","op":"set","actor":"runner/wayfinder@win-5","set":{"status":"in_progress","assignee":"runner/wayfinder@win-5"}}
{"id":"01M1EMYJEQ148CSBD8PCK27SJS","ts":"2026-09-01T14:11:12.471Z","ticket":"k0xjdm","op":"comment","actor":"runner/wayfinder@win-5","text":"## Resolution\n\n1. **Surface**: `--body-file <path|->` on all three commands, same flag name everywhere — `new`, `comment <id>` (alternative to the positional text, mutually exclusive with it), and `set <id>` (replaces a `body=` assignment, mutually exclusive with it; composes with other assignments).\n2. **stdin**: `-` reads `sys.stdin.buffer` and decodes strict UTF-8; bad bytes → usage error naming the cause. No locale guessing. Docs teach `--body-file <path>` as the primary Windows pattern (here-string → file → flag); the pipe-encoding caveat for Windows PowerShell 5.1 goes in the Windows playbook ([win-11]).\n3. **Precedence/errors**: `--body` + `--body-file` together → usage error (exit 2). Explicit file beats a template's `body` default (existing explicit-beats-template rule). Empty file → empty body (valid). Missing file / undecodable bytes → usage error (exit 2).\n\nImplementation lands in [win-8]; doc teaching updates in [win-8] (docs/users) and [win-11]/[win-12] (skills)."}
{"id":"01M1EMYJJC8MRV48H05HN5J66D","ts":"2026-09-01T14:11:12.588Z","ticket":"k0xjdm","op":"set","actor":"runner/wayfinder@win-5","set":{"status":"done"},"reason":"--body-file <path|-> on new/comment/set; strict UTF-8 stdin; explicit beats template; conflicts and bad input are usage errors"}
{"id":"01M1EMYJWKVZQ2SCJSK378SFGF","ts":"2026-09-01T14:11:12.915Z","ticket":"06ywvd","op":"set","actor":"runner/wayfinder@win-5","set":{"body":"## Destination\n\n`rp` runs natively on Windows (CPython 3.14 via uv/uvx), the test suite is green on a `windows-latest` CI leg, and every `rp`-touching skill works from PowerShell, cmd, and Git Bash — no WSL. POSIX behaviour unchanged.\n\n## Notes\n\n- Plan-don't-do is overridden for the `wayfinder:task` tickets: execution is carried inside this map; the destination is a delivered change.\n- Work each task ticket in its own branch/worktree; commit every green increment; merge to main once green.\n- Windows install root for the local wrapper defaults to `%LOCALAPPDATA%\\rohrpost`; `ROHRPOST_HOME` keeps precedence.\n- Settled decisions shaping every ticket: all three shells in scope (PowerShell/cmd/Git Bash); `--body-file` replaces heredoc teaching; skill docs = inline Windows sections + one shared Windows playbook; spec §7 rewritten to the invariant (mechanism as implementation note); locking = platform split (`msvcrt`/`fcntl`), no new dependencies.\n- `rp`-touching surfaces in scope: the `rohrpost` skill, `setup-repo-skills` (its Rohrpost tracker template), `babysit-pr`, and this repo's `docs/agents/issue-tracker.md`.\n\n## Decisions so far\n\n- [research: msvcrt locking semantics](RP-acx7sx): per-handle byte-range lock works on an empty `.lock`; `LK_LOCK` is 10x1s-retry only, so blocking needs a Python loop; OS releases locks on death; no documented single-`O_APPEND`-write atomicity on Windows.\n- [research: uv + Python 3.14 on Windows runtime facts](RP-5t88jp): `Scripts/rp.exe` trampoline works from PS/cmd/Git Bash; `uvx` caches under `%LOCALAPPDATA%\\uv`; `%LOCALAPPDATA%` confirmed as per-user home; gotchas: Store python aliases, MAX_PATH, OneDrive/Defender.\n- [research: Git for Windows vs a committed JSONL event log](RP-c9tb4p): GfW defaults `autocrlf=true`; `text eol=lf` gives byte-identical cross-OS checkouts; `merge=union` safe on normalized blobs; `merge-file` is a builtin everywhere. Surprise: text-mode fd translation rewrites appends — graduated early as [win-13].\n\n- [Windows locking design](RP-sdjcft): lock 1 byte at offset 0 of a never-deleted `.lock`; `LK_LOCK`'s bounded ~10s wait fails loud; no-nest contract kept (fails on both platforms); `doctor` silent; platform-guarded import seam, Windows proven by CI.\n\n- [--body-file CLI contract](RP-k0xjdm): `--body-file <path|->` on `new`/`comment`/`set` (one flag name; `set` composes with other assignments); strict UTF-8 stdin, no locale guessing; explicit beats template; both-given and undecodable are usage errors (exit 2).\n\n## Not yet specified\n\n- Whatever the Windows CI leg exposes beyond the locking fix — temp-file, permission-bit, or path assumptions elsewhere in `src/`. Cannot be phrased sharply until [win-9] has run.\n\n## Out of scope\n\n- Developing rohrpost itself on Windows (Makefile targets, mutation runs stay POSIX-first).\n- Standalone `.exe` distribution (uvx / local wrapper is the install path).\n- WSL as a support target."}}
{"id":"01M1EN33QH5AEGVYZBZD3YTRZH","ts":"2026-09-01T14:13:41.233Z","ticket":"9657gk","op":"set","actor":"runner/wayfinder@win-6","set":{"status":"in_progress","assignee":"runner/wayfinder@win-6"}}
{"id":"01M1ENSNTDTWF21BC14FCN6DZC","ts":"2026-09-01T14:26:00.653Z","ticket":"9657gk","op":"comment","actor":"runner/wayfinder@win-6","text":"## Resolution\n\n§7 rewritten (this commit): the contract is now **the guarantee** — appends serialised under an exclusive lock on `.lock`, one write of one line, short writes rolled back, readers lock-free and torn-tail-tolerant. The hazard-table row names no syscall. Per-platform mechanisms are notes, not contract:\n\n- **POSIX**: fcntl.flock (advisory) + the demoted Linux O_APPEND atomicity evidence (E2, PIPE_BUF aside) — retained for portability, compaction, short-write backstop.\n- **Windows**: msvcrt.locking on [0,1), enforced, bounded ~10s wait failing loud, no documented O_APPEND atomicity — the lock serialises; per the [win-4] decision the bounded wait is a wait characteristic.\n\n\"Advisory\" left the contract wording (false on Windows); the dependency note (was \"fcntl.flock, correct on Linux and macOS\") now reads \"fcntl.flock / msvcrt.locking, correct on Linux, macOS and Windows\". Existing tests map 1:1 — they cite the promises, not the syscalls; win-7 lands under this wording unchanged."}
{"id":"01M1ENSNZYT21GA2HCS29DT162","ts":"2026-09-01T14:26:00.830Z","ticket":"9657gk","op":"set","actor":"runner/wayfinder@win-6","set":{"status":"done"},"reason":"§7 states the guarantee (exclusive-lock serialization, no half-lines); fcntl/msvcrt + bounded wait demoted to platform notes; advisory left the contract"}
{"id":"01M1ENSPC9NMG631ZXPV988EA6","ts":"2026-09-01T14:26:01.225Z","ticket":"06ywvd","op":"set","actor":"runner/wayfinder@win-6","set":{"body":"## Destination\n\n`rp` runs natively on Windows (CPython 3.14 via uv/uvx), the test suite is green on a `windows-latest` CI leg, and every `rp`-touching skill works from PowerShell, cmd, and Git Bash — no WSL. POSIX behaviour unchanged.\n\n## Notes\n\n- Plan-don't-do is overridden for the `wayfinder:task` tickets: execution is carried inside this map; the destination is a delivered change.\n- Work each task ticket in its own branch/worktree; commit every green increment; merge to main once green.\n- Windows install root for the local wrapper defaults to `%LOCALAPPDATA%\\rohrpost`; `ROHRPOST_HOME` keeps precedence.\n- Settled decisions shaping every ticket: all three shells in scope (PowerShell/cmd/Git Bash); `--body-file` replaces heredoc teaching; skill docs = inline Windows sections + one shared Windows playbook; spec §7 rewritten to the invariant (mechanism as implementation note); locking = platform split (`msvcrt`/`fcntl`), no new dependencies.\n- `rp`-touching surfaces in scope: the `rohrpost` skill, `setup-repo-skills` (its Rohrpost tracker template), `babysit-pr`, and this repo's `docs/agents/issue-tracker.md`.\n\n## Decisions so far\n\n- [research: msvcrt locking semantics](RP-acx7sx): per-handle byte-range lock works on an empty `.lock`; `LK_LOCK` is 10x1s-retry only, so blocking needs a Python loop; OS releases locks on death; no documented single-`O_APPEND`-write atomicity on Windows.\n- [research: uv + Python 3.14 on Windows runtime facts](RP-5t88jp): `Scripts/rp.exe` trampoline works from PS/cmd/Git Bash; `uvx` caches under `%LOCALAPPDATA%\\uv`; `%LOCALAPPDATA%` confirmed as per-user home; gotchas: Store python aliases, MAX_PATH, OneDrive/Defender.\n- [research: Git for Windows vs a committed JSONL event log](RP-c9tb4p): GfW defaults `autocrlf=true`; `text eol=lf` gives byte-identical cross-OS checkouts; `merge=union` safe on normalized blobs; `merge-file` is a builtin everywhere. Surprise: text-mode fd translation rewrites appends — graduated early as [win-13].\n\n- [Windows locking design](RP-sdjcft): lock 1 byte at offset 0 of a never-deleted `.lock`; `LK_LOCK`'s bounded ~10s wait fails loud; no-nest contract kept (fails on both platforms); `doctor` silent; platform-guarded import seam, Windows proven by CI.\n\n- [--body-file CLI contract](RP-k0xjdm): `--body-file <path|->` on `new`/`comment`/`set` (one flag name; `set` composes with other assignments); strict UTF-8 stdin, no locale guessing; explicit beats template; both-given and undecodable are usage errors (exit 2).\n\n- [Spec §7 rewrite: invariant over mechanism](RP-9657gk): §7 states the guarantee — exclusive-lock serialisation, one write one line, rollback on short writes, torn-tail-tolerant readers; fcntl/msvcrt and the bounded wait are platform notes; \"advisory\" left the contract wording.\n\n## Not yet specified\n\n- Whatever the Windows CI leg exposes beyond the locking fix — temp-file, permission-bit, or path assumptions elsewhere in `src/`. Cannot be phrased sharply until [win-9] has run.\n\n## Out of scope\n\n- Developing rohrpost itself on Windows (Makefile targets, mutation runs stay POSIX-first).\n- Standalone `.exe` distribution (uvx / local wrapper is the install path).\n- WSL as a support target."}}
{"id":"01M1EQF330PXP53EAAHZJ703AZ","ts":"2026-09-01T14:55:10.944Z","ticket":"zy1th8","op":"comment","actor":"runner/wayfinder@win-exec","text":"Scope expanded by the maintainer: the test job matrix becomes ubuntu-latest + macos-latest + windows-latest (three-OS proof, not windows-only). Lint/type/security/complexity/mutation jobs stay ubuntu-only. Fix real platform failures; justify any skip in the resolution comment."}
{"id":"01M1EQH5746T0XVWPMW2VBTYTM","ts":"2026-09-01T14:56:18.660Z","ticket":"zwesnq","op":"set","actor":"runner/impl-win7","set":{"status":"in_progress","assignee":"runner/impl-win7"}}
{"id":"01M1ES77YTZTPATNQ47JAVDCXQ","ts":"2026-09-01T15:25:50.938Z","ticket":"zwesnq","op":"comment","actor":"runner/impl-win7","text":"Implemented the [win-4] design verbatim in src/rohrpost/store.py: module-top platform seam (sys.platform == 'win32' -> msvcrt, else fcntl) with module-private _acquire/_release; Windows locks byte range [0, 1) of .lock after fh.seek(0) (past-EOF range on the empty file), blocking via LK_LOCK's CRT-paced retries (1 s apart, ~10 attempts) with exhaustion translated to StoreError naming the dir; POSIX flock behaviour byte-identical; .lock created on first mutation and never deleted; file_lock keeps its shape and releases only what it acquired; docstring sharpened (nesting deadlocks on POSIX, fails after <=10 s on Windows, enforced locks fail plain I/O through a second handle); append_event and doctor untouched. Tests: tests/test_store.py gained .lock lifecycle and release-on-exception tests; POSIX concurrency tests unchanged, no msvcrt fakes. Local make check fully green (292 passed, 2 skipped). CI run for 285d3dc: https://github.com/code-factorio/rohrpost/actions/runs/33524804722 - all four jobs success."}
{"id":"01M1ES7CFRAM1KKBRZ8AMRMAB9","ts":"2026-09-01T15:25:55.576Z","ticket":"zwesnq","op":"set","actor":"runner/impl-win7","set":{"status":"done"},"reason":"Platform-split locking landed per [win-4]: msvcrt byte-range [0,1) with LK_LOCK on Windows, flock byte-identical elsewhere; CI green on 285d3dc"}
{"id":"01M1ES8E4634VFNA22SP972G0C","ts":"2026-09-01T15:26:30.022Z","ticket":"06ywvd","op":"set","actor":"runner/impl-win7","set":{"body":"## Destination\n\n`rp` runs natively on Windows (CPython 3.14 via uv/uvx), the test suite is green on a `windows-latest` CI leg, and every `rp`-touching skill works from PowerShell, cmd, and Git Bash — no WSL. POSIX behaviour unchanged.\n\n## Notes\n\n- Plan-don't-do is overridden for the `wayfinder:task` tickets: execution is carried inside this map; the destination is a delivered change.\n- Work each task ticket in its own branch/worktree; commit every green increment; merge to main once green.\n- Windows install root for the local wrapper defaults to `%LOCALAPPDATA%\\rohrpost`; `ROHRPOST_HOME` keeps precedence.\n- Settled decisions shaping every ticket: all three shells in scope (PowerShell/cmd/Git Bash); `--body-file` replaces heredoc teaching; skill docs = inline Windows sections + one shared Windows playbook; spec §7 rewritten to the invariant (mechanism as implementation note); locking = platform split (`msvcrt`/`fcntl`), no new dependencies.\n- `rp`-touching surfaces in scope: the `rohrpost` skill, `setup-repo-skills` (its Rohrpost tracker template), `babysit-pr`, and this repo's `docs/agents/issue-tracker.md`.\n\n## Decisions so far\n\n- [research: msvcrt locking semantics](RP-acx7sx): per-handle byte-range lock works on an empty `.lock`; `LK_LOCK` is 10x1s-retry only, so blocking needs a Python loop; OS releases locks on death; no documented single-`O_APPEND`-write atomicity on Windows.\n- [research: uv + Python 3.14 on Windows runtime facts](RP-5t88jp): `Scripts/rp.exe` trampoline works from PS/cmd/Git Bash; `uvx` caches under `%LOCALAPPDATA%\\uv`; `%LOCALAPPDATA%` confirmed as per-user home; gotchas: Store python aliases, MAX_PATH, OneDrive/Defender.\n- [research: Git for Windows vs a committed JSONL event log](RP-c9tb4p): GfW defaults `autocrlf=true`; `text eol=lf` gives byte-identical cross-OS checkouts; `merge=union` safe on normalized blobs; `merge-file` is a builtin everywhere. Surprise: text-mode fd translation rewrites appends — graduated early as [win-13].\n\n- [Windows locking design](RP-sdjcft): lock 1 byte at offset 0 of a never-deleted `.lock`; `LK_LOCK`'s bounded ~10s wait fails loud; no-nest contract kept (fails on both platforms); `doctor` silent; platform-guarded import seam, Windows proven by CI.\n\n- [--body-file CLI contract](RP-k0xjdm): `--body-file <path|->` on `new`/`comment`/`set` (one flag name; `set` composes with other assignments); strict UTF-8 stdin, no locale guessing; explicit beats template; both-given and undecodable are usage errors (exit 2).\n\n- [Spec §7 rewrite: invariant over mechanism](RP-9657gk): §7 states the guarantee — exclusive-lock serialisation, one write one line, rollback on short writes, torn-tail-tolerant readers; fcntl/msvcrt and the bounded wait are platform notes; \"advisory\" left the contract wording.\n\n- [Implement platform-split locking in store.py](RP-zwesnq): msvcrt byte-range [0,1) of a never-deleted .lock with LK_LOCK on Windows; flock byte-identical elsewhere\n\n## Not yet specified\n\n- Whatever the Windows CI leg exposes beyond the locking fix — temp-file, permission-bit, or path assumptions elsewhere in `src/`. Cannot be phrased sharply until [win-9] has run.\n\n## Out of scope\n\n- Developing rohrpost itself on Windows (Makefile targets, mutation runs stay POSIX-first).\n- Standalone `.exe` distribution (uvx / local wrapper is the install path).\n- WSL as a support target."}}
{"id":"01M1ESY9KTAWKHQS1S9SZWZJDN","ts":"2026-09-01T15:38:26.298Z","ticket":"p9a9dj","op":"set","actor":"runner/impl-win13","set":{"status":"in_progress","assignee":"runner/impl-win13"}}
{"id":"01M1ETWBZSZR67DF1J0K9QZECC","ts":"2026-09-01T15:54:51.769Z","ticket":"p9a9dj","op":"comment","actor":"runner/impl-win13","text":"Implemented per ticket and docs/research/windows-git-jsonl.md: src/rohrpost/store.py now ORs _O_BINARY = getattr(os, 'O_BINARY', 0) (defined at the platform seam with the rationale comment) into append_event's os.open flags, so Windows text mode cannot rewrite appended \\n to \\r\\n — the short-write rollback byte-count math and §7's one-write-one-line invariant hold at the byte level. New test test_append_lands_exact_lf_bytes in tests/test_store.py asserts the appended line's exact bytes (encode(event) + b'\\n', no 0x0D) on both platforms. Local make check fully green (293 passed, 2 skipped). CI run for df599ad: https://github.com/code-factorio/rohrpost/actions/runs/33527254041 — all four jobs (lint/types, tests, complexity, mutation) success."}
{"id":"01M1ETWGSR21DZWGH3Z51ZYBA3","ts":"2026-09-01T15:54:56.696Z","ticket":"p9a9dj","op":"set","actor":"runner/impl-win13","set":{"status":"done"},"reason":"append_event's os.open now sets O_BINARY on Windows, so appended lines land as exact LF-only bytes; byte-exact test added, CI green on df599ad"}
{"id":"01M1ETY58HEGZSYC99GJ51DXK9","ts":"2026-09-01T15:55:50.417Z","ticket":"06ywvd","op":"set","actor":"user/evilissimo@gmail.com","set":{"body":"## Destination\n\n`rp` runs natively on Windows (CPython 3.14 via uv/uvx), the test suite is green on a `windows-latest` CI leg, and every `rp`-touching skill works from PowerShell, cmd, and Git Bash — no WSL. POSIX behaviour unchanged.\n\n## Notes\n\n- Plan-don't-do is overridden for the `wayfinder:task` tickets: execution is carried inside this map; the destination is a delivered change.\n- Work each task ticket in its own branch/worktree; commit every green increment; merge to main once green.\n- Windows install root for the local wrapper defaults to `%LOCALAPPDATA%\\rohrpost`; `ROHRPOST_HOME` keeps precedence.\n- Settled decisions shaping every ticket: all three shells in scope (PowerShell/cmd/Git Bash); `--body-file` replaces heredoc teaching; skill docs = inline Windows sections + one shared Windows playbook; spec §7 rewritten to the invariant (mechanism as implementation note); locking = platform split (`msvcrt`/`fcntl`), no new dependencies.\n- `rp`-touching surfaces in scope: the `rohrpost` skill, `setup-repo-skills` (its Rohrpost tracker template), `babysit-pr`, and this repo's `docs/agents/issue-tracker.md`.\n\n## Decisions so far\n\n- [research: msvcrt locking semantics](RP-acx7sx): per-handle byte-range lock works on an empty `.lock`; `LK_LOCK` is 10x1s-retry only, so blocking needs a Python loop; OS releases locks on death; no documented single-`O_APPEND`-write atomicity on Windows.\n- [research: uv + Python 3.14 on Windows runtime facts](RP-5t88jp): `Scripts/rp.exe` trampoline works from PS/cmd/Git Bash; `uvx` caches under `%LOCALAPPDATA%\\uv`; `%LOCALAPPDATA%` confirmed as per-user home; gotchas: Store python aliases, MAX_PATH, OneDrive/Defender.\n- [research: Git for Windows vs a committed JSONL event log](RP-c9tb4p): GfW defaults `autocrlf=true`; `text eol=lf` gives byte-identical cross-OS checkouts; `merge=union` safe on normalized blobs; `merge-file` is a builtin everywhere. Surprise: text-mode fd translation rewrites appends — graduated early as [win-13].\n\n- [Windows locking design](RP-sdjcft): lock 1 byte at offset 0 of a never-deleted `.lock`; `LK_LOCK`'s bounded ~10s wait fails loud; no-nest contract kept (fails on both platforms); `doctor` silent; platform-guarded import seam, Windows proven by CI.\n\n- [--body-file CLI contract](RP-k0xjdm): `--body-file <path|->` on `new`/`comment`/`set` (one flag name; `set` composes with other assignments); strict UTF-8 stdin, no locale guessing; explicit beats template; both-given and undecodable are usage errors (exit 2).\n\n- [Spec §7 rewrite: invariant over mechanism](RP-9657gk): §7 states the guarantee — exclusive-lock serialisation, one write one line, rollback on short writes, torn-tail-tolerant readers; fcntl/msvcrt and the bounded wait are platform notes; \"advisory\" left the contract wording.\n\n- [Implement platform-split locking in store.py](RP-zwesnq): msvcrt byte-range [0,1) of a never-deleted .lock with LK_LOCK on Windows; flock byte-identical elsewhere\n\n- [O_BINARY appends: Windows text-mode fd translation breaks the append contract](RP-p9a9dj: append fd opened with getattr-guarded O_BINARY so Windows text mode cannot rewrite \\n to \\r\\n; byte-exact LF-only append test added)\n## Not yet specified\n\n- Whatever the Windows CI leg exposes beyond the locking fix — temp-file, permission-bit, or path assumptions elsewhere in `src/`. Cannot be phrased sharply until [win-9] has run.\n\n## Out of scope\n\n- Developing rohrpost itself on Windows (Makefile targets, mutation runs stay POSIX-first).\n- Standalone `.exe` distribution (uvx / local wrapper is the install path).\n- WSL as a support target."}}
{"id":"01M1EVR1ZC0ESE3KJRKN2BP567","ts":"2026-09-01T16:09:59.020Z","ticket":"zy1th8","op":"set","actor":"runner/impl-win9","set":{"status":"in_progress","assignee":"runner/impl-win9"}}
{"id":"01M1EXRXQ9H88P5QEQ1GP6A6AJ","ts":"2026-09-01T16:45:24.585Z","ticket":"k3p76w","op":"create","actor":"runner/impl-win9","set":{"title":"Adopt git text eol=lf for .rohrpost JSONL so Windows checkouts are byte-identical","type":"task","status":"open","priority":2,"parent":"06ywvd","labels+":["wayfinder:task"],"body":"Graduated from [win-9] (RP-zy1th8), the three-OS CI leg.\n\nEvidence: docs/research/windows-git-jsonl.md (RP-c9tb4p) — the Git-for-Windows installer defaults core.autocrlf=true, so a Windows clone of THIS repo checks .rohrpost/log.jsonl out CRLF. It works (universal-newline reads, O_BINARY LF appends, checkin normalisation), but the working tree byte-differs from the blob and invites mixed endings. The union-merge tests on the windows leg pass today partly because git normalises on checkin.\n\n`.rohrpost/**/*.jsonl  text eol=lf` (plus shadow/\\*.json?) would make every OS checkout byte-identical, per the research note. Decide the exact attribute set, apply to this repo's .gitattributes, and extend paths.GITATTRIBUTES_RULES if rp init should seed it for user repos too."}}
{"id":"01M1EXSF233CEHAX0JT7AYQ4A5","ts":"2026-09-01T16:45:42.339Z","ticket":"zy1th8","op":"comment","actor":"runner/impl-win9","text":"Resolution — three-OS test matrix landed and green.\n\nCI: .github/workflows/ci.yml test job now runs ubuntu/macos/windows (name carries the OS, fail-fast: false, per-OS coverage artifact); lint/type/security/structure/complexity/mutation stay ubuntu-only.\n\nFixed what the legs exposed (no platform skips used anywhere):\n1. stats.pipe_buf raised AttributeError on Windows (no os.pathconf/PC_PIPE_BUF) — now returns the same 4096 heuristic; unblocked test_stats, test_union_merge_long_lines and E1.\n2. E1 concurrency test hard-required the fork mp-context — Windows has none; now picks spawn on win32 (verified spawn-under-pytest works on Linux first), giving Windows a true cross-process run of its msvcrt locking seam.\n3. merge_text leaked CRLF into merged bodies on Windows: Path.write_text default newline translation turned the three temp files CRLF, git merge-file passes bytes through — 8 failures (test_merge_body_fidelity, sync scenarios) fixed with newline=\"\"; temp files also pin encoding=\"utf-8\" so cp1252 can never crash on non-ASCII bodies.\nEncoding hygiene riding the same exposure class, each with a focused test where testable: merge_text/shadow/doctor/paths read+write UTF-8 explicitly (cp1252 would mangle or silently drop shadows); every git/gh subprocess decodes UTF-8; committed git files (.gitattributes/.gitignore/config.toml) written LF byte-exactly by init; cli.main pins stdout/stderr to UTF-8 so --json (ensure_ascii=False) cannot crash on a cp1252 Windows pipe (pinned by a cp1252-stream regression test).\n\nFiles: .github/workflows/ci.yml; src/{stats,merge,shadow,paths,doctor,cli,util,compact,api}.py, src/providers/github.py; tests/{test_append_integrity,test_cli,test_api,test_shadow,test_merge_body_fidelity,test_doctor}.py.\n\nRuns: 33531866033 (first leg run: windows red — the 8 CRLF failures above), 33532457266 (green: all 7 jobs success, windows+macos+ubuntu test legs). Commits 54e9615, b62a171.\n\nGraduated to RP-k3p76w (child of the map): adopt text eol=lf for .rohrpost JSONL so Windows clones of this repo are byte-identical. Noted, not ticketed: macOS fork in a multithreaded process would warn (filterwarnings=error) — latent only; the pytest process is single-threaded today."}
{"id":"01M1EXSF62EA65QSZ6YYJ6VA1E","ts":"2026-09-01T16:45:42.466Z","ticket":"zy1th8","op":"set","actor":"runner/impl-win9","set":{"status":"done"},"reason":"Three-OS test matrix green (windows+macos test legs first-time green); Windows exposures fixed without skips: pipe_buf heuristics, spawn-based E1, byte-exact merge temp files, UTF-8/committed-file codec hygiene; CI run 33532457266"}
{"id":"01M1EXSYJE8ZGSZA48D4QEHR46","ts":"2026-09-01T16:45:58.222Z","ticket":"06ywvd","op":"set","actor":"user/evilissimo@gmail.com","set":{"body":"## Destination\n\n`rp` runs natively on Windows (CPython 3.14 via uv/uvx), the test suite is green on a `windows-latest` CI leg, and every `rp`-touching skill works from PowerShell, cmd, and Git Bash — no WSL. POSIX behaviour unchanged.\n\n## Notes\n\n- Plan-don't-do is overridden for the `wayfinder:task` tickets: execution is carried inside this map; the destination is a delivered change.\n- Work each task ticket in its own branch/worktree; commit every green increment; merge to main once green.\n- Windows install root for the local wrapper defaults to `%LOCALAPPDATA%\\rohrpost`; `ROHRPOST_HOME` keeps precedence.\n- Settled decisions shaping every ticket: all three shells in scope (PowerShell/cmd/Git Bash); `--body-file` replaces heredoc teaching; skill docs = inline Windows sections + one shared Windows playbook; spec §7 rewritten to the invariant (mechanism as implementation note); locking = platform split (`msvcrt`/`fcntl`), no new dependencies.\n- `rp`-touching surfaces in scope: the `rohrpost` skill, `setup-repo-skills` (its Rohrpost tracker template), `babysit-pr`, and this repo's `docs/agents/issue-tracker.md`.\n\n## Decisions so far\n\n- [research: msvcrt locking semantics](RP-acx7sx): per-handle byte-range lock works on an empty `.lock`; `LK_LOCK` is 10x1s-retry only, so blocking needs a Python loop; OS releases locks on death; no documented single-`O_APPEND`-write atomicity on Windows.\n- [research: uv + Python 3.14 on Windows runtime facts](RP-5t88jp): `Scripts/rp.exe` trampoline works from PS/cmd/Git Bash; `uvx` caches under `%LOCALAPPDATA%\\uv`; `%LOCALAPPDATA%` confirmed as per-user home; gotchas: Store python aliases, MAX_PATH, OneDrive/Defender.\n- [research: Git for Windows vs a committed JSONL event log](RP-c9tb4p): GfW defaults `autocrlf=true`; `text eol=lf` gives byte-identical cross-OS checkouts; `merge=union` safe on normalized blobs; `merge-file` is a builtin everywhere. Surprise: text-mode fd translation rewrites appends — graduated early as [win-13].\n\n- [Windows locking design](RP-sdjcft): lock 1 byte at offset 0 of a never-deleted `.lock`; `LK_LOCK`'s bounded ~10s wait fails loud; no-nest contract kept (fails on both platforms); `doctor` silent; platform-guarded import seam, Windows proven by CI.\n\n- [--body-file CLI contract](RP-k0xjdm): `--body-file <path|->` on `new`/`comment`/`set` (one flag name; `set` composes with other assignments); strict UTF-8 stdin, no locale guessing; explicit beats template; both-given and undecodable are usage errors (exit 2).\n\n- [Spec §7 rewrite: invariant over mechanism](RP-9657gk): §7 states the guarantee — exclusive-lock serialisation, one write one line, rollback on short writes, torn-tail-tolerant readers; fcntl/msvcrt and the bounded wait are platform notes; \"advisory\" left the contract wording.\n\n- [Implement platform-split locking in store.py](RP-zwesnq): msvcrt byte-range [0,1) of a never-deleted .lock with LK_LOCK on Windows; flock byte-identical elsewhere\n\n- [O_BINARY appends: Windows text-mode fd translation breaks the append contract](RP-p9a9dj: append fd opened with getattr-guarded O_BINARY so Windows text mode cannot rewrite \\n to \\r\\n; byte-exact LF-only append test added)\n- [CI three-OS test matrix](RP-zy1th8: test job runs ubuntu+macos+windows; exposures fixed with no platform skips — Windows pipe_buf heuristic, spawn-based E1 concurrency, byte-exact UTF-8 merge temp files, committed-file/codec hygiene; eol=lf follow-up graduated to RP-k3p76w)\n\n## Not yet specified\n\n- Whatever the Windows CI leg exposes beyond the locking fix — temp-file, permission-bit, or path assumptions elsewhere in `src/`. Cannot be phrased sharply until [win-9] has run.\n\n## Out of scope\n\n- Developing rohrpost itself on Windows (Makefile targets, mutation runs stay POSIX-first).\n- Standalone `.exe` distribution (uvx / local wrapper is the install path).\n- WSL as a support target."}}
{"id":"01M1EYGDZF3E8SYB4FTZB2TH71","ts":"2026-09-01T16:58:14.895Z","ticket":"kyb9vq","op":"set","actor":"runner/impl-win8","set":{"status":"in_progress","assignee":"runner/impl-win8"}}
{"id":"01M1F05XH4DJPNCGM42CASF0WP","ts":"2026-09-01T17:27:27.524Z","ticket":"kyb9vq","op":"comment","actor":"runner/impl-win8","text":"Implemented --body-file <path|-> per the [win-5] contract. src/rohrpost/cli.py: flag wired on new/comment/set; _read_body_file() reads a path or stdin '-' and decodes strict UTF-8 (no locale guessing); _body_from_flags() enforces --body/--body-file mutual exclusion with explicit-beats-template on new; comment's positional text is now optional and mutually exclusive with the flag; set replaces a body= assignment (conflict -> usage error) and composes with other assignments, and may be the only assignment. src/rohrpost/exceptions.py: new UsageError, mapped to exit 2 in main(). 15 focused tests in tests/test_cli.py. Docs: docs/users/README.md gains a 'Multi-line bodies' section; docs/agents/issue-tracker.md heredoc gains a --body-file sibling. First CI run failed only on Windows: fixtures used write_text (CRLF translation) and an assertion matched the !r-doubled path; fixed by writing explicit bytes and matching the file name. Green: https://github.com/code-factorio/rohrpost/actions/runs/33536541783 (commits bd69c4f, b636fa3)."}
{"id":"01M1F061P5WMMQ660A5K1XMYHJ","ts":"2026-09-01T17:27:31.781Z","ticket":"kyb9vq","op":"set","actor":"user/evilissimo@gmail.com","set":{"status":"done"},"reason":"--body-file <path|-> shipped on new/comment/set with strict UTF-8, exit-2 usage errors, and Windows-green tests"}
{"id":"01M1F06EZZHGSZHDCGQQEVV8GR","ts":"2026-09-01T17:27:45.407Z","ticket":"06ywvd","op":"set","actor":"user/evilissimo@gmail.com","set":{"body":"## Destination\n\n`rp` runs natively on Windows (CPython 3.14 via uv/uvx), the test suite is green on a `windows-latest` CI leg, and every `rp`-touching skill works from PowerShell, cmd, and Git Bash — no WSL. POSIX behaviour unchanged.\n\n## Notes\n\n- Plan-don't-do is overridden for the `wayfinder:task` tickets: execution is carried inside this map; the destination is a delivered change.\n- Work each task ticket in its own branch/worktree; commit every green increment; merge to main once green.\n- Windows install root for the local wrapper defaults to `%LOCALAPPDATA%\\rohrpost`; `ROHRPOST_HOME` keeps precedence.\n- Settled decisions shaping every ticket: all three shells in scope (PowerShell/cmd/Git Bash); `--body-file` replaces heredoc teaching; skill docs = inline Windows sections + one shared Windows playbook; spec §7 rewritten to the invariant (mechanism as implementation note); locking = platform split (`msvcrt`/`fcntl`), no new dependencies.\n- `rp`-touching surfaces in scope: the `rohrpost` skill, `setup-repo-skills` (its Rohrpost tracker template), `babysit-pr`, and this repo's `docs/agents/issue-tracker.md`.\n\n## Decisions so far\n\n- [research: msvcrt locking semantics](RP-acx7sx): per-handle byte-range lock works on an empty `.lock`; `LK_LOCK` is 10x1s-retry only, so blocking needs a Python loop; OS releases locks on death; no documented single-`O_APPEND`-write atomicity on Windows.\n- [research: uv + Python 3.14 on Windows runtime facts](RP-5t88jp): `Scripts/rp.exe` trampoline works from PS/cmd/Git Bash; `uvx` caches under `%LOCALAPPDATA%\\uv`; `%LOCALAPPDATA%` confirmed as per-user home; gotchas: Store python aliases, MAX_PATH, OneDrive/Defender.\n- [research: Git for Windows vs a committed JSONL event log](RP-c9tb4p): GfW defaults `autocrlf=true`; `text eol=lf` gives byte-identical cross-OS checkouts; `merge=union` safe on normalized blobs; `merge-file` is a builtin everywhere. Surprise: text-mode fd translation rewrites appends — graduated early as [win-13].\n\n- [Windows locking design](RP-sdjcft): lock 1 byte at offset 0 of a never-deleted `.lock`; `LK_LOCK`'s bounded ~10s wait fails loud; no-nest contract kept (fails on both platforms); `doctor` silent; platform-guarded import seam, Windows proven by CI.\n\n- [--body-file CLI contract](RP-k0xjdm): `--body-file <path|->` on `new`/`comment`/`set` (one flag name; `set` composes with other assignments); strict UTF-8 stdin, no locale guessing; explicit beats template; both-given and undecodable are usage errors (exit 2).\n\n- [Spec §7 rewrite: invariant over mechanism](RP-9657gk): §7 states the guarantee — exclusive-lock serialisation, one write one line, rollback on short writes, torn-tail-tolerant readers; fcntl/msvcrt and the bounded wait are platform notes; \"advisory\" left the contract wording.\n\n- [Implement platform-split locking in store.py](RP-zwesnq): msvcrt byte-range [0,1) of a never-deleted .lock with LK_LOCK on Windows; flock byte-identical elsewhere\n\n- [O_BINARY appends: Windows text-mode fd translation breaks the append contract](RP-p9a9dj: append fd opened with getattr-guarded O_BINARY so Windows text mode cannot rewrite \\n to \\r\\n; byte-exact LF-only append test added)\n- [CI three-OS test matrix](RP-zy1th8: test job runs ubuntu+macos+windows; exposures fixed with no platform skips — Windows pipe_buf heuristic, spawn-based E1 concurrency, byte-exact UTF-8 merge temp files, committed-file/codec hygiene; eol=lf follow-up graduated to RP-k3p76w)\n\n- [Implement --body-file in the CLI](RP-kyb9vq: --body-file <path|-> on new/comment/set with strict UTF-8 stdin, exit-2 usage errors, explicit-beats-template, and Windows-green tests)\n\n## Not yet specified\n\n- Whatever the Windows CI leg exposes beyond the locking fix — temp-file, permission-bit, or path assumptions elsewhere in `src/`. Cannot be phrased sharply until [win-9] has run.\n\n## Out of scope\n\n- Developing rohrpost itself on Windows (Makefile targets, mutation runs stay POSIX-first).\n- Standalone `.exe` distribution (uvx / local wrapper is the install path).\n- WSL as a support target."}}
{"id":"01M1F0ZPTFV0Y572RDE8CKTFT6","ts":"2026-09-01T17:41:32.623Z","ticket":"x3f018","op":"set","actor":"runner/impl-win10","set":{"status":"in_progress","assignee":"runner/impl-win10"}}
{"id":"01M1F2C5ZWXGHQ2R971W4VE1N9","ts":"2026-09-01T18:05:49.948Z","ticket":"x3f018","op":"comment","actor":"runner/impl-win10","text":"Added text eol=lf to the JSONL gitattributes rules so .rohrpost/log.jsonl and .rohrpost/archive/*.jsonl normalise to LF blobs at checkin and check out with the blob's exact bytes on any platform, regardless of core.autocrlf (shape per docs/research/windows-git-jsonl.md; merge=union untouched, since union merges now always see LF blobs).\n\nFiles touched:\n- src/rohrpost/paths.py: GITATTRIBUTES_RULES first two rules now 'merge=union text eol=lf'; write_gitattributes docstring documents the append-upgrades semantics (gitattributes later-lines-win per attribute).\n- .gitattributes (this repo): same four rules plus a short rationale comment.\n- src/rohrpost/doctor.py: existing check already iterates GITATTRIBUTES_RULES, so a repo lacking the eol rule now fails the gitattributes check. Decision (inline, per ticket): kept it a hard finding, no separate warning tier — a missing rule breaks byte-exact cross-OS sync and 'rp init' repairs it in one step; YAGNI on a warn state.\n- src/rohrpost/cli.py + docs/spec/ROHRPOST-SPEC.md (§4, §10.1) + docs/users/README.md: wording updated from 'union-merge rules' to merge and line-ending rules so docs stay truthful.\n- tests/test_gitattributes_eol.py (new): git check-attr pins merge/text/eol for both patterns; real-git round-trip under core.autocrlf=true proves CRLF working tree -> LF blob -> byte-identical LF checkout -> identical fold (sensitivity verified: without the attribute autocrlf=true yields CRLF); pre-eol .gitattributes upgrade path stays idempotent; drift guard tying this repo's .gitattributes to paths.py.\n- tests/test_doctor.py: regression that a pre-eol file fails doctor.\n\nWindows round-trip note: git's attribute semantics are platform-independent; the check-attr + autocrlf=true round-trip on Linux pins the same behaviour a Windows checkout gets.\n\nCI: run 33539932063 (2a27a97) failed Mutation testing only — the drift-guard test assumed tests/../.gitattributes which mutmut's mutants/ tree lacks; fixed by walking up from the test file in 0e22636. Final run 33540374368 (0e22636) fully green: lint, 3 OS test jobs, complexity, mutation."}
{"id":"01M1F2C8NDW9JRA3XMBFHZGDX1","ts":"2026-09-01T18:05:52.685Z","ticket":"x3f018","op":"set","actor":"user/evilissimo@gmail.com","set":{"status":"done"},"reason":"text eol=lf added to the .rohrpost JSONL gitattributes rules (paths.py + this repo), doctor flags repos missing it, round-trip pinned by real-git tests; CI fully green"}
{"id":"01M1F2D633YWWKSKWC39A6TKNH","ts":"2026-09-01T18:06:22.819Z","ticket":"06ywvd","op":"set","actor":"user/evilissimo@gmail.com","set":{"body":"## Destination\n\n`rp` runs natively on Windows (CPython 3.14 via uv/uvx), the test suite is green on a `windows-latest` CI leg, and every `rp`-touching skill works from PowerShell, cmd, and Git Bash — no WSL. POSIX behaviour unchanged.\n\n## Notes\n\n- Plan-don't-do is overridden for the `wayfinder:task` tickets: execution is carried inside this map; the destination is a delivered change.\n- Work each task ticket in its own branch/worktree; commit every green increment; merge to main once green.\n- Windows install root for the local wrapper defaults to `%LOCALAPPDATA%\\rohrpost`; `ROHRPOST_HOME` keeps precedence.\n- Settled decisions shaping every ticket: all three shells in scope (PowerShell/cmd/Git Bash); `--body-file` replaces heredoc teaching; skill docs = inline Windows sections + one shared Windows playbook; spec §7 rewritten to the invariant (mechanism as implementation note); locking = platform split (`msvcrt`/`fcntl`), no new dependencies.\n- `rp`-touching surfaces in scope: the `rohrpost` skill, `setup-repo-skills` (its Rohrpost tracker template), `babysit-pr`, and this repo's `docs/agents/issue-tracker.md`.\n\n## Decisions so far\n\n- [research: msvcrt locking semantics](RP-acx7sx): per-handle byte-range lock works on an empty `.lock`; `LK_LOCK` is 10x1s-retry only, so blocking needs a Python loop; OS releases locks on death; no documented single-`O_APPEND`-write atomicity on Windows.\n- [research: uv + Python 3.14 on Windows runtime facts](RP-5t88jp): `Scripts/rp.exe` trampoline works from PS/cmd/Git Bash; `uvx` caches under `%LOCALAPPDATA%\\uv`; `%LOCALAPPDATA%` confirmed as per-user home; gotchas: Store python aliases, MAX_PATH, OneDrive/Defender.\n- [research: Git for Windows vs a committed JSONL event log](RP-c9tb4p): GfW defaults `autocrlf=true`; `text eol=lf` gives byte-identical cross-OS checkouts; `merge=union` safe on normalized blobs; `merge-file` is a builtin everywhere. Surprise: text-mode fd translation rewrites appends — graduated early as [win-13].\n\n- [Windows locking design](RP-sdjcft): lock 1 byte at offset 0 of a never-deleted `.lock`; `LK_LOCK`'s bounded ~10s wait fails loud; no-nest contract kept (fails on both platforms); `doctor` silent; platform-guarded import seam, Windows proven by CI.\n\n- [--body-file CLI contract](RP-k0xjdm): `--body-file <path|->` on `new`/`comment`/`set` (one flag name; `set` composes with other assignments); strict UTF-8 stdin, no locale guessing; explicit beats template; both-given and undecodable are usage errors (exit 2).\n\n- [Spec §7 rewrite: invariant over mechanism](RP-9657gk): §7 states the guarantee — exclusive-lock serialisation, one write one line, rollback on short writes, torn-tail-tolerant readers; fcntl/msvcrt and the bounded wait are platform notes; \"advisory\" left the contract wording.\n\n- [Implement platform-split locking in store.py](RP-zwesnq): msvcrt byte-range [0,1) of a never-deleted .lock with LK_LOCK on Windows; flock byte-identical elsewhere\n\n- [O_BINARY appends: Windows text-mode fd translation breaks the append contract](RP-p9a9dj: append fd opened with getattr-guarded O_BINARY so Windows text mode cannot rewrite \\n to \\r\\n; byte-exact LF-only append test added)\n- [CI three-OS test matrix](RP-zy1th8: test job runs ubuntu+macos+windows; exposures fixed with no platform skips — Windows pipe_buf heuristic, spawn-based E1 concurrency, byte-exact UTF-8 merge temp files, committed-file/codec hygiene; eol=lf follow-up graduated to RP-k3p76w)\n\n- [Implement --body-file in the CLI](RP-kyb9vq: --body-file <path|-> on new/comment/set with strict UTF-8 stdin, exit-2 usage errors, explicit-beats-template, and Windows-green tests)\n- [eol=lf protection for .rohrpost JSONL](RP-x3f018: text eol=lf on the log.jsonl and archive/*.jsonl rules gives byte-identical cross-OS checkouts; doctor fails repos missing it)\n\n## Not yet specified\n\n- Whatever the Windows CI leg exposes beyond the locking fix — temp-file, permission-bit, or path assumptions elsewhere in `src/`. Cannot be phrased sharply until [win-9] has run.\n\n## Out of scope\n\n- Developing rohrpost itself on Windows (Makefile targets, mutation runs stay POSIX-first).\n- Standalone `.exe` distribution (uvx / local wrapper is the install path).\n- WSL as a support target."}}
{"id":"01M1F3D2ZZJYQP34T4KZFB769H","ts":"2026-09-01T18:23:48.223Z","ticket":"rf1841","op":"set","actor":"runner/impl-win11","set":{"status":"in_progress","assignee":"runner/impl-win11"}}
{"id":"01M1F626K5JQ0NKW81KRVKJR5C","ts":"2026-09-01T19:10:17.189Z","ticket":"rf1841","op":"comment","actor":"runner/impl-win11","text":"Windows pass for the rohrpost skill delivered in three commits (2cbacb6..980f2db).\n\nFiles touched:\n- .agents/skills/rohrpost/scripts/rohrpost.ps1.template (new): validates the install, resolves .venv\\Scripts\\rp.exe with extensionless .venv\\Scripts\\rp fallback, default home %LOCALAPPDATA%\\rohrpost, ROHRPOST_HOME precedence, prints the install-local pointer on every failure; arg passthrough + exit-code passthrough; never bypasses the wrapper. No placeholder to substitute — the default resolves at runtime, so materialisation is a plain copy.\n- .agents/skills/rohrpost/scripts/rohrpost.cmd.template (new): same contract in batch syntax (setlocal, 1>&2 pointer messages, exit /b %ERRORLEVEL%).\n- .agents/skills/rohrpost/SKILL.md: compact Windows section at the point of use (per-shell wrapper resolution in Invocation; %LOCALAPPDATA% default + ROHRPOST_HOME contract); Creating/Updating now teach --body-file (- for stdin) with a PowerShell here-string example instead of the heredoc-in-$(cat) pattern.\n- .agents/skills/rohrpost/playbooks/windows.md (new): shared Windows playbook — uv install (official installer, PATH refresh), clone + pin main, uv python install 3.14 + uv sync, wrapper materialisation (copy for .ps1/.cmd, placeholder substitution for the Git Bash wrapper from Git Bash), validation (Parser::ParseFile as the bash -n equivalent, Test-Path as the test -x equivalent, cmd validated end-to-end, doctor from each shell), execution-policy note, MAX_PATH/OneDrive/Store-python gotchas.\n- .agents/skills/rohrpost/playbooks/install-local.md: Windows branch now points at playbooks/windows.md instead of duplicating.\n- .agents/skills/rohrpost/scripts/test-rohrpost-wrapper.sh: drives all three candidate layouts (bin/rp, Scripts/rp, Scripts/rp.exe) so Git Bash on Windows runs the same self-test; header note records that .ps1/.cmd are covered by pytest + the playbook.\n- tests/test_windows_wrapper_templates.py (new): structural drift-guards (ROHRPOST_HOME precedence, default home, rp.exe-before-rp order, pointer, no uvx/uv run) on every platform; runtime tests execute the materialised wrappers via pwsh on all CI OSes (POSIX fake rp.exe, Windows the real trampoline rp.exe from the venv) and via cmd on Windows.\n- src/rohrpost/fold.py + tests/test_fold.py (fix cycle): fold normalises set body=\"\" to None so fold and snapshot agree (the CI flake in test_set_body_file_empty_clears_body was the two read paths diverging on NTFS mtime tick granularity); test walk-up fix so mutmut's mutants/ tree finds the skill dir.\n\nSkill validation: tessl skill lint on a packaged temp copy reports the plugin valid (tessl review requires auth, unavailable here).\n\nCI: run 33544991788 (bc79749) red on windows-latest (two wrong version-string asserts + the pre-existing empty-body flake) and mutation (mutants/ tree lacked the skill dir); fixed in e81a295 and 980f2db. Final run 33546521466 (980f2db) fully green: lint, 3 OS test legs, complexity, mutation."}
{"id":"01M1F62A0CXGQGBSM893F8SX7R","ts":"2026-09-01T19:10:20.684Z","ticket":"rf1841","op":"set","actor":"user/evilissimo@gmail.com","set":{"status":"done"},"reason":"PowerShell/cmd wrapper templates, SKILL.md Windows + --body-file sections, shared windows.md playbook with install-local pointer, layout-covering wrapper self-test and CI-pinned pytest coverage; final run 33546521466 fully green"}
{"id":"01M1F62RBKV4V1VWYSBA0C0YH2","ts":"2026-09-01T19:10:35.379Z","ticket":"06ywvd","op":"set","actor":"user/evilissimo@gmail.com","set":{"body":"## Destination\n\n`rp` runs natively on Windows (CPython 3.14 via uv/uvx), the test suite is green on a `windows-latest` CI leg, and every `rp`-touching skill works from PowerShell, cmd, and Git Bash — no WSL. POSIX behaviour unchanged.\n\n## Notes\n\n- Plan-don't-do is overridden for the `wayfinder:task` tickets: execution is carried inside this map; the destination is a delivered change.\n- Work each task ticket in its own branch/worktree; commit every green increment; merge to main once green.\n- Windows install root for the local wrapper defaults to `%LOCALAPPDATA%\\rohrpost`; `ROHRPOST_HOME` keeps precedence.\n- Settled decisions shaping every ticket: all three shells in scope (PowerShell/cmd/Git Bash); `--body-file` replaces heredoc teaching; skill docs = inline Windows sections + one shared Windows playbook; spec §7 rewritten to the invariant (mechanism as implementation note); locking = platform split (`msvcrt`/`fcntl`), no new dependencies.\n- `rp`-touching surfaces in scope: the `rohrpost` skill, `setup-repo-skills` (its Rohrpost tracker template), `babysit-pr`, and this repo's `docs/agents/issue-tracker.md`.\n\n## Decisions so far\n\n- [research: msvcrt locking semantics](RP-acx7sx): per-handle byte-range lock works on an empty `.lock`; `LK_LOCK` is 10x1s-retry only, so blocking needs a Python loop; OS releases locks on death; no documented single-`O_APPEND`-write atomicity on Windows.\n- [research: uv + Python 3.14 on Windows runtime facts](RP-5t88jp): `Scripts/rp.exe` trampoline works from PS/cmd/Git Bash; `uvx` caches under `%LOCALAPPDATA%\\uv`; `%LOCALAPPDATA%` confirmed as per-user home; gotchas: Store python aliases, MAX_PATH, OneDrive/Defender.\n- [research: Git for Windows vs a committed JSONL event log](RP-c9tb4p): GfW defaults `autocrlf=true`; `text eol=lf` gives byte-identical cross-OS checkouts; `merge=union` safe on normalized blobs; `merge-file` is a builtin everywhere. Surprise: text-mode fd translation rewrites appends — graduated early as [win-13].\n\n- [Windows locking design](RP-sdjcft): lock 1 byte at offset 0 of a never-deleted `.lock`; `LK_LOCK`'s bounded ~10s wait fails loud; no-nest contract kept (fails on both platforms); `doctor` silent; platform-guarded import seam, Windows proven by CI.\n\n- [--body-file CLI contract](RP-k0xjdm): `--body-file <path|->` on `new`/`comment`/`set` (one flag name; `set` composes with other assignments); strict UTF-8 stdin, no locale guessing; explicit beats template; both-given and undecodable are usage errors (exit 2).\n\n- [Spec §7 rewrite: invariant over mechanism](RP-9657gk): §7 states the guarantee — exclusive-lock serialisation, one write one line, rollback on short writes, torn-tail-tolerant readers; fcntl/msvcrt and the bounded wait are platform notes; \"advisory\" left the contract wording.\n\n- [Implement platform-split locking in store.py](RP-zwesnq): msvcrt byte-range [0,1) of a never-deleted .lock with LK_LOCK on Windows; flock byte-identical elsewhere\n\n- [O_BINARY appends: Windows text-mode fd translation breaks the append contract](RP-p9a9dj: append fd opened with getattr-guarded O_BINARY so Windows text mode cannot rewrite \\n to \\r\\n; byte-exact LF-only append test added)\n- [CI three-OS test matrix](RP-zy1th8: test job runs ubuntu+macos+windows; exposures fixed with no platform skips — Windows pipe_buf heuristic, spawn-based E1 concurrency, byte-exact UTF-8 merge temp files, committed-file/codec hygiene; eol=lf follow-up graduated to RP-k3p76w)\n\n- [Implement --body-file in the CLI](RP-kyb9vq: --body-file <path|-> on new/comment/set with strict UTF-8 stdin, exit-2 usage errors, explicit-beats-template, and Windows-green tests)\n- [eol=lf protection for .rohrpost JSONL](RP-x3f018: text eol=lf on the log.jsonl and archive/*.jsonl rules gives byte-identical cross-OS checkouts; doctor fails repos missing it)\n\n- [rohrpost skill Windows pass: wrappers + playbook](RP-rf1841): per-shell wrappers (%LOCALAPPDATA% default, ROHRPOST_HOME precedence), --body-file teaching, shared windows.md playbook, wrapper self-test over all three venv layouts\n\n## Not yet specified\n\n- Whatever the Windows CI leg exposes beyond the locking fix — temp-file, permission-bit, or path assumptions elsewhere in `src/`. Cannot be phrased sharply until [win-9] has run.\n\n## Out of scope\n\n- Developing rohrpost itself on Windows (Makefile targets, mutation runs stay POSIX-first).\n- Standalone `.exe` distribution (uvx / local wrapper is the install path).\n- WSL as a support target."}}
